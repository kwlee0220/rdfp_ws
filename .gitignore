# ROS 2 colcon build outputs
build/
install/
log/

# Stray colcon outputs accidentally created inside packages
src/*/build/
src/*/install/
src/*/log/

# Python cache / bytecode / coverage
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Backup files
*.bak
*.bak-*
*.orig
*.backup

# rosbag / large binaries (workspace-local data)
*.bag
*.mcap
*.mp4

# Workspace-local config (사용자별 절대 경로 / DSN 들어감 — 공유 금지).
# 패키지 안의 sample 은 정상 추적; 워크스페이스 root 의 인스턴스만 제외.
# /dataset_config.yaml
# /rosbag_config.yaml
# /rdfp_panda_mock.yaml

# Ansible 인벤토리 — 평문 자격증명(비밀번호) 포함, 커밋 금지.
/ansible/inventory.ini

# 셸 환경 파일 — RDFP_DB_DSN 평문 비밀번호 포함, 커밋 금지.
/.rdfprc

# 펑션베이 시뮬레이터 — 벤더 요청 항목

> **근거** [functionbay_open_work.md](functionbay_open_work.md) ·
> **설계** [functionbay_backend_design.md](functionbay_backend_design.md)
>
> **갱신 이력과 현재 상태 (2026-09-08).** 본문은 네 차례 갱신을 거쳐 **현행이다.**
>
> - **2026-08-31** — 솔버가 `펑션베이` → `서울대 Crisp` 로 교체되어 근거 수치가 바뀌었다 (§0.1, 이력).
> - **2026-09-03** — **A-3 · B-7 · B-10 및 `velocity`/`effort` 해결**, **신규 A-5(주기적 정지)** (§0.2).
> - **2026-09-04** — B 단계 전수 측정. **A-4 · B-1 강등**(재현 없음 / 개선 확정), **B-4 철회**,
>   **신규 B-11 · B-12 및 `effort` 의미 문의** (§0.3).
> - **2026-09-08** — **B-12 해결 확인**(TF 발행 반영), **신규 B-13**(`/clock` 시간축 통일).
> - **2026-09-09** — **B-11 발송.** 같은 날 추가 실측 3건을 §B-11 "추가 관측" 에 붙였다 (발송분 포함 여부는 확인 필요).
> - **2026-09-11** — **신규 B-14**(`r2` 에 `peg ↔ 탁자` 접촉이 없어 고정물 밖에 내려놓으면 물체가 자유낙하한다).
>
> **발송 상태** — **A-5 는 2026-09-03, B-11 은 2026-09-09 발송(둘 다 회신 대기).**
> 미발송: **B-13 · `effort` 의미 문의**.
>
> **살아 있는 요청은 A-1 · A-5 · B-2 · B-8 · B-9 · B-11 · B-13 · B-14** 이며(**A-2 는 2026-09-11 철회** — `Kp` 가 씬 XML 에 이미 있었다), A-4 는
> 강등됐으나 질문 하나가 회신 대기로 남아 있다. **닫힌 항목(A-3 · B-1 · B-3 · B-4 ·
> B-5 · B-6 · B-7 · B-10 · B-12)은 [부록 E](#closed) 에
> 번호를 유지한 채 모았다** — 본문 절은 취소선 제목으로 그대로 남아 있다.
>
> **각 항목에 `솔버` 를 표기했다 (2026-09-09).** 시뮬레이터가 솔버 두 가지
> (**RecurDyn** = 문서의 "펑션베이 솔버", **Crisp** = "서울대 솔버")를 쓰므로, 어느
> 쪽에서 관측된 것인지 밝히지 않으면 어느 코드를 봐야 할지 알 수 없다. **2026-08-31
> 3차 측정부터 지금까지 Crisp 이고, 09-01 이후 도출 항목은 예외 없이 Crisp 다.**
> 회차별 귀속과 미확정 항목: [부록 F](#solver).
>
> 시뮬레이터 쪽에서만 고칠 수 있는 항목을 모아 **요청 단위로** 정리한 문서다.
> 각 항목은 "무엇을 · 왜 우리가 못 고치나 · 어떻게 확인할 것인가(수용 기준)"
> 세 가지를 갖춘다. 실측 근거는 전부 위 문서에 있고 여기서는 절 번호로 가리킨다.

---

## 0. 요약

**가장 중요한 것은 A-1 중력 보상 하나다.** 나머지는 없어도 데이터 수집을 시작할 수
있지만, A-1 이 없으면 **명령(action)과 실제(observation)가 자세에 따라 계통적으로
어긋난 채로** 데이터가 쌓인다 (open_work §3.5).

> **위 판단은 펑션베이 솔버 기준이다.** 서울대 솔버에서는 처짐이 10배 줄어 A-1 의 정착
> 오차가 작아졌으나, 2026-09-01 에 **오차가 이동 방향에 따라 부호를 바꾼다**는 것이
> 확인되어 **A-1 은 시급성을 유지한다** — 방향 의존은 우리 쪽 보정으로 흡수되지 않는다.
> **B-1(응답 속도)은 승격**되고, **A-4(고착)는 재판정이 필요하다** — 접촉·부하로 못
> 움직이는 경우가 고착과 똑같이 보이므로 과거 사례에 오진이 섞였을 수 있다. §0.1 을 본다.

**항목 번호는 종결된 뒤에도 재사용하지 않는다.** 닫힌 항목(A-3 · B-1 · B-3 · B-4 ·
B-5 · B-6 · B-7 · B-10 · B-12)은 번호를 유지한 채 [부록 E](#closed)
로 옮겼다 — 회신·메일에서 옛 번호로 지칭될 수 있기 때문이다.

**`솔버` 열은 그 항목이 어느 솔버에서 관측된 것인지를 가리킨다** — 판단 근거는
[부록 F](#solver) 에 있다.

| | 항목 | 솔버 | 없으면 생기는 일 | 상태 |
|:-:|---|:-:|---|---|
| **[A-1](#a-1)** | **중력 보상 (피드포워드)** | **Both** | ⚠ **2026-09-11: 우리 쪽에서 상쇄 중이다** (도달 오차 0.00 mm) — 시급성은 내려갔으나 **요청은 유지한다**(우리 보상은 부하·속도 성분을 못 잡는다). 반영되면 우리 보상을 꺼야 한다 — 안 끄면 이중 보정.<br> action↔observation 계통 편향. **2026-09-03 재측정에서 절반으로 줄었으나 방향 의존은 남았다** (아래 A-1 §최신) | 미발송 |
| ~~[A-2](#a-2)~~ | ~~제어기 사양 공개~~ | Both | **해결 (2026-09-11) — 물을 필요가 없었다.** `Kp`·`Kd` 가 **씬 XML 에 이미 있었다** (`<씬>__Manipulator_1_vm.xml` 의 `<Gain Kp="2000 ×7" Kd="200 ×7">`; t1·r2 동일). 그 값으로 중력 보상을 구현해 도달 오차 12.37 → **0.00 mm** 를 실증했다 | **철회** |
| [A-4](#a-4) | "명령 무반응 고착" 문의 | **Both** | **강등 (2026-09-04).** 재판정에서 **재현되지 않았다**(7/7 반응 ×2). 과거 사례 중 그리퍼 건은 **우리 타입 버그**였다 — **새로 보낼 사례가 없다.** 질문("알려진 현상인가")만 남긴다 | 회신 대기 |
| **[A-5](#a-5)** | **34초 주기 2초 정지 제거** | **Crisp** | **원격 조작이 34초마다 2초씩 끊긴다.** 시연 데이터를 정상적으로 수집할 수 없다 | **발송 · 회신 대기** |
| [B-2](#b-2) | 게인·보상 스위치 노출 | **Both** | 게인 튜닝을 매번 벤더 빌드에 의존 (B-1 은 닫혔지만 스위치는 여전히 없다) | 미발송 |
| **[B-9](#b-9)** | **정확한 관절 한계 사양** | **Crisp** | URDF·실제 스펙·시뮬레이터 실효값이 **셋 다 다르다.** 계획은 되는데 실행이 못 미치는 영역이 생긴다 | 미발송 |
| **[B-11](#b-11)** | **`t1` 씬에 손끝 접촉 정의 추가** | **Crisp** | 파지는 되는데 **들면 떨어진다.** peg-in-hole 성공 에피소드를 만들 수 없다 | **발송 · 회신 대기** |
| **[B-13](#b-13)** | **`/clock` 과 `/output/*` 의 시간축 통일** | **Crisp** | `/clock` 은 **경과시간**, `/output/*`·카메라는 **벽시계** — 56.7년 어긋나 `use_sim_time` 을 켤 수 없다. sim time 의 이점을 하나도 못 쓴다 | 미발송 |
| **[B-8](#b-8)** | **Panda 링크 메시 `link0~7.obj`** | Both<br>*(솔버 무관)* | 대상 URDF 가 참조하는데 없어 **로드가 완결되지 않는다**. MoveIt 충돌 기하가 실물과 달라진다 | 미발송 |
| — | [그리퍼 `effort` 의 의미](#a-4) | **Crisp** | `Kp × 잔차`라 **접촉력을 못 읽는다.** 접촉 채널이 따로 있는지 묻는다 (A-4 문의 5번에 포함) | 미발송 |

### ~~0.1~~ 서울대 솔버로 교체되며 달라진 것 — **이력 (본문 반영 완료)**

> **이 표대로 본문을 손보는 작업은 끝났고, A-5 는 2026-09-03 에 발송했다.**
> 아래 두 칸은 이후 측정에서 **뒤집혔다** — ① **B-1 "τ 242 ms 악화"** → τ 199.9 ms 로
> **개선**(§0.3) ② **A-4 "3차에서도 재현"** → 재판정에서 **재현 없음**, 그 사례는 우리
> 스크립트의 타입 버그였다(§A-4 ④). 최신 값은 §0.3 과
> [functionbay_checklist.md](functionbay_checklist.md) 의 A·B 표를 본다.

| 항목 | 펑션베이 솔버 근거 | **서울대 솔버 실측** | 조치 |
|---|---|---|---|
| **A-1** 중력 보상 | joint4 오차 **-0.0983 rad (5.6°)**, 자세 의존 12배 | **-0.0097 rad (0.56°)**, 자세 의존 11배. **추가로 방향 의존 확인** — 상승 +20 mm 지령에 +10.0 mm, 하강 −20 mm 지령에 −30 mm | **수치 교체.** 정착 오차는 줄었으나 **방향 의존은 정적 보정표로 흡수되지 않는다** — 하향했던 우선순위를 되돌린다 |
| **A-4** 고착 문의 | 2차에서 재현 | **3차에서도 재현.** 엔드포인트 재시작으로 복구 안 됨, `/input/endeffector` 도 무반응 → Unity 수신 경로로 범위 축소. **단, 접촉·부하로 못 움직이는 경우가 같은 증상을 낸다** | **재판정 후 발송.** 접촉과 구별되는 사례만 남긴다 (판정 절차는 A-4 절) |
| **B-1** `K_p` 상향 | τ **133 ms** | **τ 242 ms — 1.8배 악화** | **승격.** 강성이 올라간 만큼 감쇠도 올라간 것으로 보인다. "정확도를 유지한 채 τ 를 줄여 달라"로 요구를 다시 쓴다 |
| **B-4** `/output/endeffector` 복구 | 토픽 없음 | 2차→3차 **발행 확인** → **2026-09-01 다시 발행 없음** (15 초 무수신, peg-in-hole 모델) | **유지 · 회귀 보고.** "어떤 조건에서 발행되는가" 로 질문을 바꾼다 |
| **B-6** 그리퍼 출력 사양 | `Float64MultiArray` 6개, 의미 불명 | **`sensor_msgs/JointState` 6관절 × q/v/f 로 변경됨.** 옛 형식은 2 finger × (q,v,f) 였음이 확인됨. ⚠ **2026-09-10 정정 — 이것은 `t1` 씬 한정이다** (r2 는 2관절·도·p/v/f) | **삭제 (해결됨).** 사양은 backend_design **§6.1a** |
| — | (없음) | **보고 주기 50 → 10 → 30 Hz** (조정됨) | **신규 항목 필요.** 상한이 얼마인지, `/output/*` 와 카메라가 예산을 나눠 쓰는지 확인 |
| — | (없음) | **카메라 9.6 Hz 로 일정** (관절 보고 주기와 무관) | **B-10 으로 정식화됨** |
| — | (없음) | **`/output/endeffector` 의 기준 프레임이 우리 URDF 와 다름** (`panda_link8` 대비 Z축 +90° · Z −23.4 mm) | **신규 항목 필요.** TCP 정의(원점·축 방향)와 자세 표현 규약(RPY 순서)을 문서로 요청 |
| **A-4** | "명령 무반응 고착" 문의 | **판정 도구 결함으로 과거 사례 일부가 오진** (joint7 실효 상한 1.6387 rad 에 걸린 것을 전체 무반응으로 읽음) | **문의 전 재판정 필요.** 고친 도구로 재현되는 사례만 남겨 보낸다 |
| — | (없음) | **관절 한계가 URDF 와 불일치** (joint7 상한 1.6387 vs URDF 3.15, joint4 상한 부호도 스펙과 반대) | **B-9 로 정식화됨** |
| **B-5** | 카메라 `rgb8` 발행 | **해결됨** — `rgba8` → **`bgr8`** 로 전환 확인 (step 2560→1920, 대역폭 −25%). `cv_bridge` 기본 형식이라 변환 불필요 | **삭제** |
| **A-3** | 카메라 `header.stamp` | **부분 해결** — 값이 채워지기 시작했으나 **시간 기준이 다르다** (`sec=63` = 기동 후 경과 vs 관절 토픽의 벽시계 epoch) | **요청 변경**: "채워 달라" → **"관절 토픽과 같은 벽시계 기준으로 맞춰 달라"** |
| — | (없음) | **`velocity`·`effort` 미발행** — `TCP_정의.txt` 상 솔버는 `q·v·f` 를 모두 계산한다 | **신규 항목 필요.** 발행만 하면 된다. `effort` 는 Peg-in-Hole 접촉 판정에 직결 |
| — | (없음) | **대상 URDF·메시 확보됨** — 개정본(관절 한계 3건 정정)과 Robotiq 메시 10종(coarse+fine)을 `panda_ftsensor_robotiq` 패키지로 정리 | 남은 요청은 **Panda 링크 메시 `link0~7.obj` 8종**과 **SRDF**(없으면 우리가 작성) |
| A-3 카메라 `stamp` | `stamp = 0` | **변화 없음** | 그대로 |
| B-7 시계 동기 | 2.22 초 오프셋 | 미재측정 | 그대로 (재확인 권장) |

**요약(당시): A-1 은 상당 부분 해소, B-1 은 악화, B-4 는 해결, 보고 주기 항목은 신규.**
본문 반영은 완료됐다. B-1·A-4 판정은 이후 뒤집혔다 — 위 주의 참조.

### 0.2 2026-09-03 재측정으로 달라진 것

측정 도구를 `fb_stamp.py` 로 정밀화하면서 **그동안 '지터'로 읽던 현상의 정체가
드러났고**, 동시에 요청 항목 네 개가 해결됐다. 근거는
[functionbay_checklist.md](functionbay_checklist.md) §A-17 과 `misc/fb_stamp/*.json`.

| 항목 | 이전 | **2026-09-03 실측** | 조치 |
|---|---|---|---|
| **A-3** 카메라 stamp | 기동 후 경과시간 | **전 토픽 wall-clock.** 카메라−관절 차 **−13.4 ms** 로 일정 (발행 주기 3배 바꿔도 불변) | **삭제 (해결).** 남은 −13.4 ms 는 파이프라인 고정 지연이라 우리가 상수로 보정한다 |
| **B-10** 카메라 주기 설정 | 9.606 Hz 고정으로 판단 | **XML 설정값이었다** — `Publish_Frequency`·`Frequency` 를 10→5 로 바꾸니 4.98 Hz | **삭제 (해결).** 애초에 요청할 필요가 없었다 |
| **B-7** 시계 동기 | 고정 오프셋 2.22 s | **폐기.** 같은 머신이고 stamp 는 wall-clock 이다. 오프셋이 흔들리던 원인은 시계가 아니라 **정지+과속 톱니**였다 | **삭제** |
| 신규 `velocity`·`effort` | 빈 배열 | **발행됨** (`pos=7 vel=7 eff=7`) | **삭제 (해결).** B-14 파지 판정이 성립한다 |
| ~~**B-4**~~ `/output/endeffector` | 무발행 회귀 | **발행 재개** (30 Hz). 타입 `PoseStamped`, **`frame_id` 빈 문자열**, 쿼터니언 순서 미확정 | **삭제 (철회, 09-04).** 09-03 에는 "값 규약을 알려 달라"로 바꿨으나, **우리 스택이 이 값을 쓰지 않으므로** 규약도 필요 없다 |
| 보고 주기 상한 | 미회신 | sim 시각 기준 발행률이 설정값과 **정확히 일치** (30.008 / 14.999 Hz) | 사실상 해소. 별도 문의 불필요 |
| — | (없음) | **34.3초 주기로 2.0초 정지.** 발행 주기와 무관하게 재현 | **신규 A-5.** 현재 미해결 중 가장 크다 |
| — | (없음) | **정지 사이 6~7% 상시 과속** (캐치업 아님) | **A-5 에 포함** |

**요약: 요청 4건이 닫히고, 그보다 큰 신규 항목 A-5 가 열렸다.**

### 0.3 2026-09-04 전수 측정으로 달라진 것

체크리스트 **B 단계 16항목을 전수 실행**하고 우리 쪽 오진 두 건을 밝힌 결과다. 근거는
[functionbay_checklist.md](functionbay_checklist.md) §B 표와 §2026-09-04 절.

| 항목 | 이전 | **2026-09-04 실측** | 조치 |
|---|---|---|---|
| **A-4** 고착 문의 | "3차에서도 재현" | **재현 없음** — `fb_raw` 2회 모두 **7/7 관절 반응**(이동률 95~102%), `name` 유무 동일 | **새로 보낼 사례가 없다.** 질문 자체는 회신 대기로 유지 |
| **B-1** `K_p` 상향 | "τ 242 ms — 악화" | **τ 199.9 ms** (기준선 252~259). 09-04 오전 179.8 ms 와 함께 **개선 확정** | **강등.** §0.1 의 '악화' 판정은 무효 |
| **B-3** 관절 한계 클램프 | 해결 | joint4 상한 **+0.0698** 을 **4자리까지 재현** | **해결 유지** |
| **B-9** 관절 한계 사양 | 09-03 근거 | **비대칭이 확정됐다** — joint7 은 URDF 상한 2.9671 을 **넘어 +3.1500**, joint4 는 URDF **안쪽** 0.0698 에서 클램프 | **유지 · 근거 보강** |
| **A-1** 중력 보상 | 비대칭 9.73 mm | **9.87 mm** (상승 +15.13 / 하강 −25.00) — 두 회차가 같아 **정체** | **유지.** 20.0 → 9.8 mm 에서 멈췄다 |
| ~~B-4~~ · ~~frame_id 규약~~ | 값 규약 문의 | — | **철회** — `/output/endeffector` 를 **쓰지 않기로 확정**했다 |
| — | (없음) | **파지가 이동 중 유지되지 않는다** — `t1` 씬에 손끝 접촉 정의가 없다 | **신규 B-11** (2026-09-09 발송) |
| — | (없음) | **물체 pose 를 받을 채널이 없다** — `/scene/objects` 를 채울 수 없다 | **신규 B-12** (문안 작성 완료·미발송) |
| — | (없음) | 그리퍼 `effort` 가 **접촉 토크가 아니라 `200 × 잔차`** (모델 `Kp=200`) | **신규 문의** (미발송) |
| **A-5** 34초 정지 | 신규 | 길이 **0.89~0.99 s** 관측(기록 2.04 s 보다 짧다), 주기 **33.2 s** | **2026-09-03 발송 · 회신 대기** |

**요약: A-4·B-1 두 건의 판정이 뒤집혀 강등되고, B-4 는 철회, 신규 3건이 열렸다.**
**시뮬레이터 쪽 최대 미해결은 여전히 A-5 다.**

> **정지 중 명령 처리는 우리 쪽에서 답할 수 없다** (A-5 메일 4-1 질문). 단발 명령은
> 지령값 그대로 적용되지만, **정지 동안 우리 Python 프로세스가 실행되지 못해**
> (`spin_once` 가 최대 945 ms 블록, 스레드 분리해도 동일 — 체크리스트 §0 함정 8)
> 한 침묵 안에 여러 명령을 넣는 시험이 불가능하다. **회신을 기다리는 쪽을 택했다.**

---

## A. 필수 요청

<a id="a-1"></a>
### A-1. 중력 보상 (피드포워드)

**무엇을** — 관절 명령 토크에 현재 자세의 중력 토크를 더해 달라.

```
토크 = K_p·(목표 − 실제) + K_d·(−속도) + τ_g(q)      ← τ_g 항 추가
```

**현재 증상** — 시뮬레이터는 중력 보상이 없는 유한 강성 위치 제어로 동작한다.
중력이 끌어내리는 힘과 제어기가 밀어 올리는 힘이 같아지는 지점에서 멈추므로
**정상상태 오차가 원리적으로 0 이 될 수 없다.**

```
K_p × 오차 = 중력 토크        →        오차 = 중력 토크 / K_p
```

**추가 근거 (2026-09-01) — 오차의 부호가 이동 방향을 탄다.** 같은 자세에서도
올릴 때와 내릴 때 결과가 반대다. **2026-09-03 재측정에서 절반으로 줄었으나 구조는
그대로다** — 여전히 올릴 때 부족하고 내릴 때 초과한다.

| 이동 | 지령 | 2026-09-01 | **2026-09-03** | |
|---|---|---|---|---|
| EE 상승 | +20 mm | **+10.0 mm** (50%) | **+15.20 mm** (76%) | 중력이 거슬러 **부족** |
| EE 하강 | −20 mm | **−30.0 mm** (150%) | **−24.92 mm** (125%) | 중력이 도와 **초과** |
| **비대칭** | | **20.0 mm** | **9.73 mm** | 절반으로 감소 |

**2회 반복이 소수점 둘째 자리까지 같았다** (15.20 / −24.92). 잡음이 아니라 결정론적
계통 오차다. `ready` 자세에서 `scripts/functionbay/fb_ee.py` 로 측정했다.

이것이 중요한 이유는 **우리 쪽에서 보정할 수 없는 성질**이기 때문이다. 자세 의존
편향은 자세별 보정표로 흡수할 여지가 있지만, 방향 의존 편향은 같은 목표점에
어느 쪽에서 접근했는지에 따라 값이 달라져 **정적 보정표로 흡수되지 않는다.**
한 방향에서 얻은 보정 계수를 반대 방향에 적용하면 실제로 발진했다(±30 mm 왕복).

부수 증상으로, **한 관절만 지령해도 나머지가 하중을 받아 밀린다** — `joint2` 를
+0.0277 rad 움직였을 때 FK 예측 −14.02 mm 대비 실측은 −1.06 mm 였고, 유지 지령을
받은 `joint4`·`joint6` 이 움직여 상쇄한 결과다.

발산이 아니라 정착이다 — 고정 목표를 15 초간 명령하면 **1.5 초 안에 수렴한 뒤
소수점 4 자리까지 미동도 없다** (open_work §3.2).

**이것이 버그가 아니라 중력 때문임을 보이는 실측 두 개** (open_work §3.3):

| 자세 | joint2 오차 | joint4 오차 | **joint1** | **joint7** |
|---|---|---|---|---|
| `ready` (접힘) | +0.0122 | **-0.0983** | **+0.0000** | **-0.0000** |
| 중간 | +0.0809 | -0.0968 | **-0.0000** | **-0.0000** |
| 펴짐 (수평쪽) | **+0.1428** | -0.0840 | **-0.0000** | **+0.0001** |

1. **joint2 오차가 팔을 펼수록 12 배로 커진다** (0.0122 → 0.1428). 모멘트 암이
   길어져 중력 토크가 커진 것이다 — `K_p` 는 그대로이고 분자만 커졌다.
2. **회전축이 수직이라 중력 토크가 0 인 joint1 · joint7 은 세 자세 모두 정확히
   0.0000 이다.** 분자가 0 이면 오차도 0 이다.

즉 **남은 오차는 전부 중력에서 온다.** `ready` 자세 최악은 joint4 의
**-0.0983 rad ≈ 5.6°** 다.

**왜 우리가 못 고치나** — 우리는 위치 명령만 보낼 수 있다. 오차가 `τ_g(q)/K_p` 이므로
명령 쪽에서 없애려면 예상 처짐을 미리 더해 보내야 하는데, 그러려면 `K_p` 값과
시뮬레이터의 질량 모델이 필요하다 (→ **A-2**). 시뮬레이터 안에서 하면 한 단계다.

**구현 힌트** (참고용, 판단은 벤더 몫)

| 방법 | 처짐 | 부작용 |
|---|---|---|
| **중력 토크 피드포워드** (권장) | **원리적으로 제거** | 없음 — 질량 모델만 있으면 된다 |
| 로봇 링크만 `useGravity = false` | 제거 | 접촉·파지 힘의 사실성 상실. **임시방편** |
| `K_p` 상향 | 감소할 뿐 0 은 안 된다 | 진동·발산 위험 (→ B-1 로 분리) |

- Unity `ArticulationBody` 기반이라면 `GetJointGravityForces()` 로 중력 토크를 받아
  매 `FixedUpdate` 에 `AddJointForce()` 로 상쇄하는 것이 정석이다. 중력은 scene 에 남고
  로봇만 보상된다.
- RecurDyn 계열 솔버라면 역동역학의 중력항을 그대로 쓰면 된다.

**수용 기준** — 우리가 `fb_hold.py 15` 로 검증한다 (§C).

| 항목 | 현재 | 요청 후 |
|---|---|---|
| joint4 정착 오차 (`ready`) | **-0.0983 rad (5.6°)** | **절대값 ≤ 0.005 rad** |
| joint2 오차의 자세 의존성 | 0.0122 → 0.1428 (**12 배**) | 자세 무관, 모두 ≤ 0.005 |
| joint1 · joint7 | 0.0000 (이미 정상) | 유지 |
| 수렴 시간 | 1.5 초 | 악화 없을 것 |

`0.005 rad ≈ 0.29°` 는 현재 joint1 · joint7 의 변동폭(0.0001 수준)에 비해 충분히
여유 있는 값이라, 모델 오차가 남아도 통과할 수 있는 선으로 잡았다.

<a id="a-2"></a>
### ~~A-2. 제어기 사양 공개~~ — **철회 (2026-09-11)**

> **물을 필요가 없었다.** 요청 내용이 *"관절별 `K_p`·`K_d` 를 알려 달라 — 그러면
> 우리가 중력 토크를 계산해 목표값을 미리 보정할 수 있다"* 였는데, 그 값이 **이미
> 씬 XML 안에 있었다**:
>
> ```xml
> <!-- <씬>__Manipulator_1_vm.xml — t1·r2 동일 -->
> <Gain Kp="2000 2000 2000 2000 2000 2000 2000" Kd="200 200 200 200 200 200 200" />
> ```
>
> 그 `Kp` 로 `Δq = −τ_g(q)/Kp` 를 계산해 지령에 더했더니 **`ready` 예측이 실측과
> 일곱 관절 전부 0.03 mrad 안에서 맞았고**, 작업 영역 8지점 도달 오차가
> **12.37 → 0.00 mm** 가 됐다. 요청의 목적이 달성됐으므로 철회한다.
>
> 상세: [functionbay_gravity_compensation.md](functionbay_gravity_compensation.md).
> 아래는 이력이다.

#### (이력) A-2. 제어기 사양 공개

**A-1 이 거절되더라도 이것만은 받아야 한다.** 알면 우리 쪽에서 명령을 보정할 수 있다.

- 제어 방식 — **PD 인가 PID 인가.** 특히 **적분항이 있는가** (있는데도 정상상태
  오차가 남는다면 원인이 중력이 아니라 다른 데 있다는 뜻이다)
- `K_p` / `K_d` 의 실제 값과 단위
- 물리 스텝 주기 · 명령 수신 주기 · **내부 보간 유무**
  (현재는 보간이 없다고 보고 우리가 50 Hz 로 선형 보간해 밀어 넣는다)
- 중력 모델의 링크 질량 · 무게중심 — **URDF 와 같은 값인가**

`K_p` 와 질량 모델만 있으면 `τ_g(q)/K_p` 를 우리가 계산해 명령에 미리 얹는 우회가
가능하다. 정확도는 시뮬레이터 내부 보상보다 떨어지지만 5.6° 를 방치하는 것보다 낫다.

<a id="a-3"></a>
### ~~A-3. 카메라 `header.stamp`~~ — **해결됨 (2026-09-03)**

> **2026-09-03 재측정에서 전 토픽이 wall-clock 으로 확인되어 이 항목은 닫는다.**
> 카메라와 관절의 stamp 차는 **−13.4 ms** 로, 발행 주기를 30/10 → 15/5 Hz 로 바꿔도
> −0.0135 → −0.0133 s 로 변하지 않는다. 즉 카메라 파이프라인의 **고정 지연**이므로
> 우리 쪽에서 상수로 보정한다. 아래는 회귀 감시용 기록이다.
>
> 다만 `camera_info` 는 여전히 없다 (절 끝 참조).

**부분 해결됐다.** 미설정(0)이던 스탬프가 채워지기 시작했다. 다만 **관절 토픽과
시간 기준이 다르다.**

```
/camera_image        stamp: sec=63          ← 시뮬레이터 기동 후 경과 시간으로 보인다
/output/panda_joint  stamp: sec=1788146994  ← 벽시계 epoch
```

데이터셋은 에피소드 분할과 토픽 간 정렬을 전부 `header.stamp` 로 한다. **기준이
다르면 두 토픽을 짝지을 수 없어**, 스탬프가 채워졌어도 이미지를 시간축에 놓지
못하는 것은 그대로다.

**요청**: 카메라 스탬프를 `/output/panda_joint` 와 **같은 기준(벽시계 epoch)** 으로
발행해 달라. 값은 프레임을 렌더링한 시각이면 된다.

**수용 기준**

| 항목 | 현재 | 목표 |
|---|---|---|
| 시간 기준 | 기동 후 경과 (`sec=63`) | **관절 토픽과 동일한 벽시계 epoch** |
| 관절 스탬프와의 차 | 비교 불가 | **1 프레임 주기 이내** |

`camera_info` 에 해당하는 토픽도 없다. 카메라 내부 파라미터를 쓰는 경로가 생기면
필요해지므로, 발행 계획이 있는지 함께 확인한다.

<a id="a-4"></a>
### A-4. "명령 무반응 고착" 문의 — 알려진 현상인가 — **재판정 완료 (2026-09-04)**

> **재현되지 않았다.** 고친 `fb_raw.py` 로 2026-09-03·04 두 차례 관측해 **7/7 관절이 반응**했고
> (이동률 95~102%) `name` 유무 양쪽이 동일했다. 과거 사례 중 **그리퍼만 죽어 보인 건은 우리
> 스크립트의 메시지 타입 버그**였다(아래 ④). **따라서 새로 보낼 사례가 없다** — 질문 자체는
> "알려진 현상인가"로 남기되, 우선순위를 낮춘다.
>
> 아래는 당시 관측 기록이다.

**요청이 아니라 질문이다.** 상태 보고는 정상인데(49.9 Hz) **명령만 전혀 먹지 않는**
상태로 빠지는 것을 두 차례 관측했다 (open_work §2.2).

- 상태 보고는 계속 나온다 → **살아 있는지 여부로는 판정할 수 없다**
- 50 Hz 램프(`fb_recover.py`)로도 빠져나오지 못한다 → **Unity 재시작이 필요하다**
- 보고 주기 지터가 커진다 (정상 13~23 ms → 고착 시 8.8~32.8 ms)

> ⚠️ **발송 전 재판정 필요 (2026-09-01).** 이 현상과 **구별되지 않는 상황**이 있다 —
> **접촉·부하로 움직이지 못하는 경우**다. 실제로 한 번 오진했다: 그리퍼가 닫힌 채
> 무언가에 눌린 상태에서 하강 명령이 먹지 않아 "고착"으로 읽었는데, `joint1`(중력
> 토크가 없는 축)에 단독 명령을 넣으니 정상 응답(+0.0526 / 지령 +0.05)이었다.
>
> (한때 "명령을 멈추면 팔이 처져서 같은 증상을 낸다"고 적었으나 **그것은 오진이었고
> 철회한다** — 실측 결과 setpoint 가 latch 되어 25~30초간 자세를 유지한다.
> backend_design §4.3.)
>
> **판정 절차**: 무반응 의심 시 (1) 그리퍼·팔이 무언가에 닿아 있지 않은지 보고,
> (2) `joint1` 또는 `joint7` 에 단독 명령을 넣어 응답을 본다. 그 축이 응답하면
> 고착이 아니다. 이 절차로 재현되는 사례만 남겨 보낸다.
> **이 절차를 `scripts/functionbay/fb_cmdpath.py` 로 자동화했다** — 아래 ④가
> 그것으로 얻은 첫 사례다.

우리 쪽에서 트리거를 좁히려 다음을 시험했으나 **다섯 조건 모두 재현에 실패**했다 —
100/200/500 Hz × 5 초, 500 Hz × 15 초 + 목표 12 회 전환, 관절 한계 초과 명령
(joint4 → -3.36). 즉 **발행률도 한계 초과 명령도 트리거가 아니다.**

#### ④ 2026-09-03 — 그리퍼만 죽은 사례는 **A-4 가 아니었다** (원인: **우리 스크립트**)

> ## ⚠️ 2026-09-04 정정 — 아래 진단의 결론이 틀렸다
>
> **실제 원인은 우리 스크립트의 메시지 타입 버그였다.** `fb_gripper.py` 와
> `fb_cmdpath.py` 가 `/input/gripper_joint` 에 `sensor_msgs/JointState` 를 발행했는데
> 시뮬레이터는 `std_msgs/Float64MultiArray` 로 구독한다. ROS 2 는 같은 이름에 다른
> 타입의 퍼블리셔를 허용하고 **연결하지 않되 오류도 내지 않으므로**, 증상이 정확히
> "명령을 무시한다"로 보인다. 두 스크립트는 수정했다.
>
> **따라서 아래 "무반응" 표 다섯 줄은 무효다** — 명령이 시뮬레이터에 도달한 적이 없다.
> 같은 이유로 *"차트가 잡고 있으므로 ROS 명령이 이길 수 없다"* 는 **시험된 적이 없는
> 추론**이다. 팔이 반응한 것은 팔 쪽 타입(`JointState`)이 맞았기 때문이다 — 대조군이
> 성립하지 않았다.
>
> **차트가 잡고 있었다는 부분은 살아남는다.** 관측값 `0.65002 rad` 이 차트 주석의
> `theta=0.65 rad` 와 일치하고 부호 패턴도 맞았다. 그것은 타입과 무관한 독립 증거다.
> 다만 현재 모델에서는 재현되지 않으며, `s = 0.15` ~ `0.725` 를 지령대로 도달한다.
>
> **`Kp=200` 은 예상보다 중요했다.** 아래 "설정 정상" 항목이 기록한 그 값이
> `/output/gripper_joint` 의 `effort` 를 그대로 설명한다 — 실측
> `effort = 200.0 × (지령 − 실제)` (18점, 오차 0.037 이하). 즉 **`effort` 는 접촉
> 토크가 아니라 제어 오차항**이고, 아래 *"빈손 0.004 / 파지 17.19"* 는 접촉력비가
> 아니라 잔차비다. 파지 판정을 이 값으로 재구성할 수 없다 (체크리스트 B-14).
>
> **재발 방지 순서** — 그리퍼 무반응을 만나면 A-4 나 차트를 의심하기 **전에**
> `ros2 topic info /input/gripper_joint` 로 타입과 퍼블리셔 수를 먼저 본다.

> **이 항목은 벤더에 보내지 않는다.** 처음에는 "그리퍼 입력 경로만 죽은 A-4 변종"
> 으로 판단했으나, 추적 결과 **원인이 시뮬레이터가 아니라 우리가 쓰는 모델 설정**
> 이었다. 여기 남기는 이유는 두 가지다 — ① 같은 증상을 다시 만났을 때 A-4 로
> 오진하지 않기 위해, ② **A-4 판정 시 반드시 배제해야 할 원인**이기 때문이다.

**원인** — 활성 모델 `t1__panda_robotiq_ref_1_vm.xml` 의 입력이
`<Input Type="SignalChart"> <SignalChart Chart_File_Name="panda_peginhole_file" />`
이고, 그 차트의 `<Gripper>` 절이 **t=4 초부터 마지막까지 ±37.242257°를 유지**한다.
차트 파일 주석이 직접 밝힌다 — `theta=0.65 rad = 37.242257 deg`. 실측 그리퍼 관절값은
**0.65002 rad**, 부호 패턴도 `[+,+,−,−,−,+]` 로 일치했다. **차트가 잡고 있으므로 ROS
그리퍼 명령이 이길 수 없다.**

**A-4 와 구별되는 결정적 차이 — Unity 재시작으로 낫지 않는다.** A-4 의 유일한 알려진
복구 수단이 재시작인데, 이 증상은 재시작 후에도 그리퍼가 같은 값(`s=0.6424`)에 같은
접촉(6.70 → 7.34 N·m)으로 되돌아왔다. **재시작으로 안 나으면 A-4 가 아니다.**

관측 기록은 아래에 남긴다 — 판정 절차의 예시로서 유효하다. 같은 시각에 **팔이 정확히
반응**했다는 것이 접촉·부하 가설을 배제해 준 부분은 그대로 쓸모가 있다.

| 시험 | 결과 |
|---|---|
| 그리퍼 `s=0` (완전 개방), `name` 채움 | **무반응** (이동 0.000000 rad) |
| 그리퍼 `s=0`, `name` 비움 | **무반응** |
| 그리퍼 `s=0.60` (현재 0.65 에서 작은 변화) | **무반응** |
| 그리퍼 `s=0.70` (**반대 방향 = 더 닫기**) | **무반응** |
| 그리퍼 현재값 그대로 재지령 | **무반응** |
| **팔 `joint7` +0.030** (중력 토크 0) | **지령 +0.030 → 실제 +0.030002** |
| **팔 `joint1` +0.020** | 실제 +0.012259 (부하로 부족하나 반응) |
| 팔 `joint7`, `name` 비움 | +0.030003 |

동시에 확인한 전제 조건 — **전부 정상이었다.**

- **시뮬레이션은 진행 중이었다.** `header.stamp` 가 정상 진행하고 `velocity`·`effort`
  는 매 메시지 갱신됐다. 멈춘 것이 아니다.
- **`/input/gripper_joint` 구독자 1개.** Unity 는 연결되어 있었다.
- **그리퍼 관절 위치 변동폭 `6e-8 rad`** — 250건을 50 Hz 로 밀었는데 수치적으로 완전 고정.
- **설정 정상.** `t1__Gripper6_1_vm.xml` 이 `Gripper_Torque` / `Kp=200` / `Kd=20` /
  `gripper_joint1~6` 매핑을 갖고 있다.
- **그리퍼는 물체와 접촉 중이었다** (`|effort|` 최대 6.70 N·m, 빈손 0.004 / 파지 17.19).
  다만 **여는 방향과 닫는 방향이 모두 무반응**이므로 물체에 막힌 것으로 설명되지 않는다.

**설명이 안 되는 부분이 남는다** — 같은 차트가 `<Arm>` 절도 갖는데 **팔은 ROS 명령에
반응했다.** 차트가 끝난 뒤(마지막 waypoint t=11 초) 팔은 풀리고 그리퍼는 안 풀리는
것으로 보이나, `Input Type` 의 정확한 의미와 ROS 입력과의 우선순위는 문서가 없어
확인할 수 없다. **이것만 벤더에 묻는다** (아래 4·5번).

#### A-4 판정 절차 — ④ 이후 갱신

무반응을 A-4 로 올리기 전에 **세 가지를 배제**한다.

1. **접촉인가** — 그리퍼 `|effort|` > 1 N·m 이면 무언가에 닿아 있다.
2. **차트가 잡고 있는가** — `VariableModel/t1__*_vm.xml` 의 `<Input Type>` 이 가리키는
   차트를 열어, 해당 축의 유지값(도 단위)이 관측값과 같은지 본다. **같으면 A-4 가 아니다.**
3. **재시작으로 낫는가** — A-4 는 재시작으로 복구된다. 안 나으면 다른 원인이다.

`scripts/functionbay/fb_cmdpath.py` 가 1·2번에 필요한 값(effort, 관절값의 도 단위 표기)을
함께 내고, 팔을 대조군으로 같이 시험한다.

**묻고 싶은 것**

1. 알려진 현상인가. 재현 조건이 있는가
2. 고착 여부를 밖에서 판정할 수 있는 신호(로그·토픽·상태 플래그)가 있는가
3. Unity 재시작 없이 복구하는 방법이 있는가
4. **`Input Type="SignalChart"` 와 ROS 입력이 같은 축에 걸리면 어느 쪽이 이기는가.**
   차트가 끝난 뒤 축이 풀리는가, 마지막 값을 계속 유지하는가
5. ~~팔과 그리퍼가 이 점에서 다르게 동작하는 이유가 무엇인가~~ — **삭제 (09-04).**
   근거였던 "팔은 받고 그리퍼는 안 받았다"가 **우리 스크립트의 타입 버그**였다.
   대신 물을 것: **`effort` 가 접촉 토크가 아니라 `Kp × 잔차` 인데, 접촉력을 관측할
   채널이 따로 있는가** (체크리스트 B-14)
6. **ROS 로만 구동하려면 모델을 어떻게 설정해야 하는가.** `Input Type` 에 어떤 값이
   있는지, 차트의 특정 절(예: `<Gripper>`)만 비활성화할 수 있는지

---

<a id="a-5"></a>
### A-5. 34초 주기 2초 정지 제거 (2026-09-03 신규) — **발송 완료 · 회신 대기**

**무엇을** — 시뮬레이터가 **약 34초마다 약 2초간 완전히 멈춘다.** 원인 확인과 제거를 요청한다.

**이 항목은 2026-09-03 에 벤더로 발송했다.** 문안 전문은
[functionbay_vendor_mail_20260903_stall.md](functionbay_vendor_mail_20260903_stall.md)
에 있고, 같은 메일의 요청 3항이 **정지 사이 상시 과속**(1.06~1.07배)의 의도 여부도 묻는다.
여기에는 요청 단위로 필요한 것만 싣는다.

**측정** (180초 연속 관측, 2026-09-03)

| 항목 | 설정 30 Hz / 10 Hz | 설정 15 Hz / 5 Hz |
|---|---|---|
| 정지 주기 | **34.32 s** (34.16~34.48) | **34.58 s** (34.38~34.93) |
| 정지 시간 | **2.04 s** (1.88~2.23) | **2.32 s** (2.13~2.63) |
| 손실 비율 | 5.65 % | 6.44 % |
| 정지 사이 진행률 | 1.063× | 1.072× |
| sim 시각 기준 발행률 | 30.008 Hz (설정값) | 14.999 Hz (설정값) |

**발행 주기를 절반으로 낮춰도 정지 주기는 1% 미만 변한다** — ROS 2 발행 부하가 원인이
아니므로 우리 쪽에서 주기를 낮춰 해결할 수 없다.

**시뮬레이터 측 현상이라는 근거 넷**

1. 정지 중 `header.stamp` 가 한 프레임(0.034 s)밖에 진행하지 않는다. 전송 지연이나
   버퍼링이면 stamp 는 계속 진행해야 한다 → 시뮬레이션 자체가 멈춘 것이다.
2. 네 토픽이 **7~88 ms 안에 동시에** 멈춘다. 토픽별 전송 문제가 아니다.
3. 수신측(WSL2) 시계를 Windows 호스트와 대조했고 진행률 차이는 **0.4% 이내**로,
   관측된 6.3% 과속을 설명하지 못한다.
4. 정지를 제외하면 sim 시각 기준 발행률이 설정값과 정확히 일치한다.

**함께 확인된 것 — 정지 사이의 상시 과속.** 정지 사이 구간에서 시뮬레이션 시각이
실시간보다 6~7% 빠르게 진행한다. 구간을 4등분해 재면 `1.0587 1.0583 1.0584 1.0583`
으로 **처음부터 끝까지 일정**해, 정지를 만회하는 단기 캐치업이 아니다. 그러면서도
장기 평균은 1.000 에 수렴하므로 (0.9996~1.0003) 훨씬 긴 시상수의 조절 기구가 따로
있는 것으로 보인다. **의도된 동작인지 확인이 필요하다.**

**왜 우리가 못 고치나** — 시뮬레이터 내부의 주기적 작업이다. 발행 주기 설정으로도
바뀌지 않고, 우리 쪽에서 관측할 수 있는 것은 "멈췄다"는 사실뿐이다.

**우리 쪽에 생기는 문제** — **데이터 결손은 아니다.** sim 시각 기준으로 stamp 는 연속이며
(5419개 구간 중 간격 0.1 s 초과 0건) 시각으로 정렬하면 끊김이 없다. 깨지는 것은
**실시간성**이다.

1. **원격 조작 중 2초 무반응** — 사람이 조작하는 시연 데이터를 수집하는데, 34초마다
   2초씩 멈추면 시연을 이어갈 수 없다. 정지 중에 보낸 명령의 처리 방식(버퍼링/유실/
   최종값만 반영)도 확인이 필요하다.
2. **접촉 동역학의 속도 불균일** — peg-in-hole 을 다루는데 6~7% 과속 구간과 정지
   구간이 번갈아 나타나면 조작자가 느끼는 물리 응답이 일정하지 않다.
3. **벽시계 기준 시간제한의 오발동** — 액션 응답 대기, 데드맨 스위치 등이 2초 정지에
   걸린다. (우리 쪽 대응: `teleop_retarget` 의 `pedal_timeout` 을 2.5 s 이상으로 잡는다.)

**요청**

1. **(우선)** 34초 주기 2초 정지의 원인 확인 및 제거. 주기가 매우 규칙적이라 가비지
   컬렉션, 주기적 체크포인트/저장, 로그 flush, 물리 엔진 재구성 등이 후보다.
2. **제거가 어렵다면** — 정지 주기·시간을 설정으로 조절할 수 있는지, 정지 시작·종료를
   알리는 신호를 낼 수 있는지.
3. **상시 과속이 의도된 보상이라면 끄는 편이 낫다.** 우리는 sim 시각 기준으로 정렬하므로
   실시간과의 평균 일치보다 **속도의 일관성**이 중요하다.

**수용 기준**

| 항목 | 현재 | 목표 |
|---|---|---|
| 180초 관측 중 0.3 s 초과 수신 공백 | **5 회** | **0 회** |
| 정지 제외 구간 시뮬레이션 진행률 | 1.063 ~ 1.072× | **1.00 ± 0.01** |

검증은 `scripts/functionbay/fb_stamp.py 180` 으로 한다.

---

## B. 후속 요청

<a id="b-1"></a>
### ~~B-1.~~ `K_p` 적정 상향 — **강등 (2026-09-04, 개선 확정)**

> **두 회차가 일치한다** — 09-03 저녁 **179.8 ms**, 09-04 전수 **199.9 ms**(범위 169~224).
> 기준선 252~259 ms 대비 개선이며, §0.1 의 "τ 242 ms 악화" 판정은 무효다. **더 요청하지 않는다.**
> twist 경로도 별도로 해결됐다(`servo_command_bridge` + 속도 스케일 0.8).
>
> **dead time 은 판정하지 않는다** — 팔 61.8 ms 와 그리퍼 63.1 ms 가 사실상 같아, 액추에이터
> 특성이 아니라 **보고 주기를 포함한 파이프라인 지연**으로 보인다(체크리스트 §2026-09-04 ②).
>
> 아래는 09-03 측정 기록이다. **τ 가 179.8 ms 로 측정됐다** (기준선 252~259 ms 대비 30% 개선). `ready` 자세에서
> joint7 계단 ±0.05 rad × 12회, 10%/63.2% 두 점으로 1차 지연을 풀었다
> (`scripts/functionbay/fb_step.py`).
>
> **다만 기준선을 낸 측정 방법이 문서에 없어 방법 차이 가능성을 배제하지 못한다.**
> 같은 회차의 dead time 은 **63.1 ms** 로 기준선(43~58)보다 커 보이지만, 기준선은
> 보고 주기 50 Hz(표본 20 ms) 시절 값이고 지금은 30 Hz(33 ms)라 **재려는 양이 표본
> 간격과 비슷해 판정을 보류한다.** 정밀히 재려면 보고 주기를 100 Hz 이상으로 올려야 한다.
>
> 아래는 기존 요청 본문이다.

#### (본문) `K_p` 적정 상향

**A-1 과 뿌리가 같지만 별개의 요청이다.** 강성은 정상상태 오차와 응답 속도 양쪽의
분모에 들어간다.

```
정상상태:  오차 = τ_g / K_p          ← A-1 이 분자를 없앤다
과도응답:  시정수 τ ≈ B / K_p        ← 분자가 없어져도 이건 그대로다
```

**중력 보상은 τ 를 줄여 주지 않는다.** 실측 τ 는 **132.6 ms** 이고, 이것이 §5 의
servo 발산과 §6 의 느린 응답의 주범이다 (open_work §6.1).

`K_d` 도 함께 올려 감쇠비를 유지해야 한다. 너무 올리면 진동하고 접촉이 딱딱해지므로
**"진동하지 않는 선까지"** 가 요청의 전부다.

**수용 기준** — joint7(중력 중립축이라 처짐이 섞이지 않는다)에 계단 ±0.04 rad 을
방향을 번갈아 8 회, 50 Hz 발행으로 측정한다.

| 항목 | 현재 | 목표 |
|---|---|---|
| 시정수 τ | **132.6 ms** (중앙 126.0 / 범위 120.5~147.8) | **≤ 50 ms** |
| 오버슈트 | — | ≤ 5% |
| 정착 후 진동 | 없음 | 없음 (유지) |
| dead time | 33.7 ms (바닥 ~25 ms) | 악화 없을 것 |

<a id="b-2"></a>
### B-2. 게인·보상 스위치를 밖으로 노출

`K_p` · `K_d` · 중력보상 on/off 를 설정 파일이나 ROS 파라미터로 빼 달라.
B-1 은 본질적으로 튜닝이라, 값을 바꿀 때마다 벤더 빌드를 기다리면 수렴하지 않는다.
노출만 되면 **"적정값 찾기"를 우리가 대신할 수 있다.**

<a id="b-3"></a>
### B-3. 관절 한계 클램프

시뮬레이터가 관절 한계를 강제하지 않는다 — joint4 에 URDF 한계(-3.1416)를 넘는
**-3.36 을 명령했더니 그대로 갔다** (open_work §2.2).

처짐과 무관해 보이지만 **같은 사고 경로의 끝**이다. 처진 값을 다시 명령하면 누적되고
(open_work §3.1), 그 끝이 MoveIt 으로 복구 불가능한 자기충돌 자세다 (open_work §4).

```
/check_state_validity → valid = False
  contacts:  panda_hand ↔ panda_link5 / panda_leftfinger ↔ panda_link5 / panda_link5 ↔ panda_link7
```

**최소한 관절 한계에서 클램프**해 주면 이 경로 하나가 막힌다. 자기충돌 검사까지는
요구하지 않는다 (우리 쪽 `/check_state_validity` 로 사전 필터가 가능하다).

<a id="b-4"></a>
### ~~B-4.~~ `/output/endeffector` — **철회 (2026-09-04)**

> **보내지 않는다.** `/ee_pose` 는 우리 스택이 `/output/panda_joint` → TF 로 직접 FK 계산하며, 시뮬레이터가 주는 `/output/endeffector` 는 **어떤 노드도 구독하지 않는다** (진단 스크립트 `fb_checkup`·`fb_stamp`·`fb_ee` 만 참조). 안 쓰는 값의 `frame_id` 와 쿼터니언 순서를 벤더에 물을 이유가 없다. 아래는 이력으로 남긴다.

> **발행은 재개됐다 (2026-09-03).** `PoseStamped` 로 30 Hz 발행 중이다. 남은 문제는
> **값을 해석할 수 없다**는 것이다.
>
> - `header.frame_id` 가 **빈 문자열**이라 어느 좌표계 기준인지 알 수 없다.
> - 타입이 문서 §7 ⑥ 의 `Float64MultiArray[6]` (XYZ + RPY) 에서 `PoseStamped` 로 바뀌었고,
>   **쿼터니언 성분 순서(xyzw / wxyz)가 확정되지 않았다.** 관측값
>   `(-0.7071, 0.7071, -0.0003, -0.0002)` 는 노름 1.0 으로 유효하지만 어느 쪽으로도 읽힌다.
> - **위치 오프셋은 2026-09-03 에 확정했다.** `ready` 자세에서 같은 순간의 두 값을
>   비교하면 `/output/endeffector` 가 `/ee_pose`(우리 FK, `panda_hand`) 대비
>   **z −23.0 mm**, x·y 는 **0.3 mm 이내로 일치**한다. 기존 기록 −23.4 mm 와 사실상
>   같은 값이 재현됐다 — 위치는 **고정 오프셋 하나로 설명된다.**
> - 남은 것은 **프레임 이름과 자세 표현**이다. `frame_id` 가 비어 있고 쿼터니언 성분
>   순서(xyzw / wxyz)가 확정되지 않았다.
>
> **요청**: `frame_id` 를 채우고, TCP 원점·축 방향과 쿼터니언 순서를 문서로 알려 달라.
> 우리 쪽에서는 C 단계에서 `/ee_pose` 와 대조해 교차 검증한다.
>
> 아래는 무발행 시절의 기록이다 (회귀 감시용).

#### (이력) `/output/endeffector` 복구

발행 여부가 측정마다 달라진다.

| 시점 | 상태 |
|---|---|
| 1차 | 전부 `0.0` 으로 고정 발행 (open_work §2.2) |
| 2차 | **토픽 자체가 사라짐** |
| 3차 (서울대 솔버) | 실제 값 발행 — `[0.505, -0.000, 0.209, 3.140, -0.001, -1.571]` |
| **2026-09-01 (peg-in-hole 모델)** | **토픽은 등록되나 15 초간 한 건도 발행되지 않음** |

한 번 해결된 것으로 판단해 닫았다가 되살아났으므로, **"고쳐 달라"가 아니라 "어떤
조건에서 발행되는가"** 로 질문을 바꾼다 — 모델(Assembly)마다 EE body 지정이 필요한
것인지, 솔버 설정에 딸린 것인지가 핵심이다. `TCP_정의.txt` 는 body 9(`robotiq_85_base_link`)
를 출력한다고 적고 있는데 실제로는 나오지 않는다.

현재는 URDF FK 로 대체하고 있어 급하지는 않지만, 시뮬레이터가 계산한 EE 값과의
교차검증 수단이 없다.

<a id="b-5"></a>
### ~~B-5. 카메라 `rgb8` 발행~~ — **해결됨 (2026-08-31)**

`rgba8` → **`bgr8`** 로 전환된 것을 확인했다. 요청할 것이 없다.

| | 이전 (`rgba8`) | **현재 (`bgr8`)** |
|---|---|---|
| 640×480 한 프레임 | 1,228,800 B | **921,600 B** (−25%) |
| `step` | 2560 | **1920** |

`bgr8` 은 `cv_bridge` · OpenCV 의 기본 형식이라 **변환 단계도 함께 사라졌다.**
원래 요청은 `rgb8` 이었으나 `bgr8` 이 오히려 낫다.

<a id="b-6"></a>
### ~~B-6. 그리퍼 출력 사양~~ — 해결됨 (2026-09-01 실측), **범위 정정 (2026-09-10)**

> ⚠️ **이 항목이 확정한 "6관절 × q/v/f" 는 빌드의 성질이 아니라 `t1` 씬의 성질이었다.**
> 같은 빌드 `MMR-Build_260907` 의 `r2` 씬은 **2관절**을 보고하고, 명령은 **도(degree)**
> 이며 **관절당 `(position, velocity, force)`** 로 채운다. 아래 본문은 `t1` 기준으로
> 읽는다.
>
> **다시 열지는 않는다** — 우리 쪽에서 규약 파일로 흡수했고(backend_design §6.1a),
> 벤더에 요청할 것이 없다. 다만 회신·후속 빌드에서 "그리퍼 형식"이 언급되면
> **어느 씬인지 함께 확인해야 한다.**

**질문할 필요가 없어졌다.** 3차 접속에서 형식이 확정됐고, 그 과정에서 1·2차 값도
해석됐다. 사양은 `functionbay_backend_design.md` §6.1 에 있다.

**1·2차 형식** — `std_msgs/Float64MultiArray`, **손가락 2개 × (각도, 속도, 토크)**:

| | raw | finger0 (q, v, f) | finger1 (q, v, f) |
|---|---|---|---|
| 1차 | `[0.000143, ~0, -14.34, -0.000140, ~0, 13.97]` | `+0.000143, 0, -14.34` | `-0.000140, 0, +13.97` |
| 2차 | `[0.7198, ~0, -0.0037, -0.7198, ~0, 0.0031]` | `+0.7198, 0, -0.0037` | `-0.7198, 0, +0.0031` |

"큰 성분이 index 2·5 에서 0·3 으로 옮겨갔다"는 것은 **구조 변경이 아니라 상태 차이**였다 —
1차는 열린 채(q≈0) 토크만 걸린 상태, 2차는 닫힌 상태(q=0.7198)다. `finger_joint` 범위
`[0, 0.725]` 에 대면 **0.02% = 완전 개방 / 99.3% = 완전 폐쇄**로 정확히 들어맞고,
좌우 q·f 가 부호만 뒤집힌 대칭인 것도 맞는다. 단위는 **rad / rad·s⁻¹ / N·m** 다.

**3차 형식 (현재)** — `sensor_msgs/JointState`, **6 관절**:

```
name[i]     = gripper_joint{i+1}          (i = 0..5)
position[i] / velocity[i] / effort[i]     각도[rad] / 각속도[rad/s] / 토크[N·m]
```

손가락당 대표 1축(2축)에서 **전 관절 6축**으로 늘었고, q/v/f 가 인터리브 대신
JointState 의 세 배열로 분리됐다. 벤더의 내부 패킹 규약(`res[3*i+0/1/2] = q/v/f`,
`TCP_정의.txt`)은 그대로이며 **관절 단위**로 적용된다.

> **오래된 bag/로그를 읽을 때 주의한다.** 같은 토픽 이름에 형식이 두 가지 존재한다.
> 길이 6 의 `Float64MultiArray` 면 옛 형식(2×qvf), `JointState` 면 새 형식(6관절)이다.

**남은 확인 사항** — 손가락 내부에서 인덱스 1·2(및 4·5)가 `inner_knuckle` /
`inner_finger` 중 어느 것인지. 부호와 크기로 추정했을 뿐이며, 파지 판정에는 영향이
없어 우선순위는 낮다.

<a id="b-10"></a>
### ~~B-10. 카메라 주기 설정~~ — **해결됨 (2026-09-03)**

> **설정 가능했다.** `MMR_Database/Database/VariableModel/t1__RGBCamera_1_vm.xml` 의
> `Publish_Frequency` 와 `<camera Frequency>` 를 10 → 5 로 바꾸니 실측 4.98 Hz 가 됐다.
> 관절 쪽도 `t1__panda_robotiq_ref_1_vm.xml` 의 `Hz` 로 같은 방식이다.
> **애초에 벤더에 요청할 항목이 아니었다.** 아래는 이력이다.

#### (이력) 카메라 주기 설정

**무엇을** — `/camera_image` 발행 주기를 설정할 수 있게 해 달라. 목표는 **5 Hz** 다.

**현재** — 9.606 Hz 로 고정이며 관절 보고 주기와 무관하다(open_work §7 ⑤). 관절
보고는 50 → 10 → 30 Hz 로 조정해 주셨는데 카메라는 그 영향을 받지 않았다.

**왜 문제인가** — 우리 레코더는 **CFR** 이라 선언한 fps 가 곧 mp4 의 시간축이 되고,
파이프라인에 데시메이션 로직이 없다. 소스 9.6 Hz 를 5 Hz 로 선언하면 **영상이 약
1.9배 빠르게 재생되어** 이미지와 관절 시계열이 어긋난다. 5 Hz 를 택한 이유는 대상
과제가 Peg-in-Hole 이라 접촉이 개입하는데, 시정수 255 ms 대비 프레임 간격 200 ms 여야
과도 응답이 프레임 사이에 묻히지 않기 때문이다.

**수용 기준** — 지정한 주기(예: 5 Hz)로 `/camera_image` 가 발행되고, 20 초 고정 구간
계수로 ±5% 안에 든다.

설정이 불가능하다면 알려 달라 — 우리 쪽에 데시메이션 노드를 넣는다. 다만 렌더를
9.6 Hz 로 돌리고 절반을 버리는 것이라 시뮬레이터 부하가 그만큼 낭비된다.

<a id="b-9"></a>
### B-9. 정확한 관절 한계 사양 — **2026-09-03 새 근거**

> **관절마다 한계 규칙이 다르다.** `ready` 에서 출발해 0.15 rad/s 로 밀며 정지점을
> 잰 결과(`scripts/functionbay/fb_limit.py`):
>
> | 관절 | URDF 한계 | 실효 정지점 | |
> |---|---|---|---|
> | joint4 상한 | +0.0873 | **+0.0698** | URDF **안쪽**에서 클램프 (4자리 재현) |
> | joint4 하한 | −3.1416 | **−3.0718** | URDF 안쪽 |
> | joint7 상한 | +2.9671 | **+3.1500** | URDF 를 **넘어선다** |
>
> **같은 로봇에서 한쪽은 강제하고 한쪽은 안 한다.** joint7 이 URDF 한계를 0.18 rad
> 넘어가는 것이 의도인지, 그렇다면 실제 사양이 무엇인지 알려 달라.
>
> (joint7 상한은 이전 기록 1.6387 rad 와 크게 다르나, **그 값을 어느 자세에서 쟀는지
> 우리 문서에 없어** 벤더 변경인지 자세 의존인지 가리지 못한다. 이번 값은 모두
> `ready` 출발 기준이다.)
>
> 아래는 기존 요청 본문이다.

#### (본문) 정확한 관절 한계 사양

**무엇을** — `panda_joint4` 와 `panda_joint7` 의 정확한 가동 범위를 알려 달라.

세 출처가 서로 다르다.

| 관절 | 벤더 URDF | 실제 Franka 스펙 | 시뮬레이터 실측 |
|---|---|---|---|
| `panda_joint4` | `-3.0718 ~ **+0.0698**` | `-3.0718 ~ **-0.0698**` | 미측정 |
| `panda_joint7` | `**±3.15**` | `±2.8973` | **상한 1.6387** |

나머지 다섯 관절은 URDF 와 스펙이 정확히 일치하므로 이 둘만 어긋난 것은 **오기로
보인다.** 특히 `panda_joint4` 는 실제 Panda 의 팔꿈치가 0 에 닿지 못하는데 URDF 는
양수까지 허용하고 있어 부호가 뒤집힌 것으로 읽힌다.

`panda_joint7` 은 더 복잡하다 — 실측 상한 **1.6387 rad** 가 URDF(3.15)보다도
스펙(2.8973)보다도 훨씬 좁다 (open_work §4.1). 이 값이 의도된 것인지, 아니면 모델
설정에서 온 부수 효과인지 확인이 필요하다.

**왜 문제인가** — MoveIt 은 URDF/설정의 한계로 계획하므로, 그보다 좁은 실효 한계가
있으면 **계획은 성공하는데 실행이 목표에 못 미친다.** 그 정지값이 정착 오차처럼
보이므로 원인을 오독하기 쉽다 — Isaac 백엔드에서 실제로 이것을 **중력 처짐으로
오독**한 적이 있다.

우리 쪽에서는 `panda_ftsensor_robotiq/config/joint_limits.yaml` 로 **좁은 쪽(실제
스펙)을 강제**해 두었다. 수집한 데이터가 실물로 가야 하기 때문이다. 다만 joint7 의
1.6387 구간은 여전히 남는다.

<a id="b-11"></a>
### B-11. `t1` 씬에 **손끝 접촉 정의**를 추가해 달라 — 2026-09-04 (**2026-09-09 발송**)

**증상.** 파지는 성공하는데 **물체를 들어 올리면 미끄러져 떨어진다.** 상승 중에는
버티다가 **정지한 뒤에** 빠지므로 정지마찰조차 부족한 것으로 보인다.

**원인으로 보이는 것.** 활성 씬 `t1` 에 **손끝 접촉 정의가 없다.** 등록된 접촉은
`Contact_1/2/3` 셋뿐이고 전부 물체 쪽이며, 셋 다 `friction_coefficient="0.15"` 다.

```
t1__Contact_1_vm.xml   peg_hole_round_16_sdf   friction_coefficient="0.15"
t1__Contact_2_vm.xml   peg_round_16_sdf        friction_coefficient="0.15"
t1__Contact_3_vm.xml   peg_tray_round_16_sdf   friction_coefficient="0.15"
```

**같은 빌드의 `r2` 씬에는 있다.** 손끝마다 전용 접촉을 걸고 마찰을 **1.0** 으로 잡는다.

```xml
<!-- r2__GGeomContact_1_vm.xml -->
<Force name="GGeomContact_1" type="Contact">
  <child  IGEOSURFNAME="peg_round_16_surf" />
  <friction D_F_C="1" S_T_V="0.01" D_T_V="0.015" S_F_C="1" />
  <normal K="1000000" C="2000" MAXPEN="1" />
  <parent JGEOSURFNAME="left_inner_finger_surf" />
</Force>
<!-- GGeomContact_2 → right_inner_finger_surf -->
```

**요청.** `t1` 에도 위와 동일한 정의를 좌·우 손끝(`left_inner_finger_surf` /
`right_inner_finger_surf`)에 추가해 달라. 같은 물체(`peg_round_16`)를 쓰는 참조 설정이
이미 빌드 안에 있으므로 확인이 쉬울 것이다.

**우리 쪽에서 배제한 것.**

| 가설 | 결과 |
|---|---|
| 파지력 부족 | `grasp` 가 이미 `finger_joint` 상한 `0.725 rad` — 더 못 조인다 |
| 이동 속도·가속 | 느리게 할수록 **오히려 일찍** 놓친다 |
| 경로 형상 | 단일 관절 이동과 손끝 기준 수직 이동 양쪽에서 재현 |
| 파지 판정 오류 | `at_goal`/`stalled` 은 매번 정상 |

**실측** — P2 자세에서 파지 후 손끝 기준 수직 +88 mm:

| 구간 | 접촉축 잔차 | 비고 |
|---|---|---|
| 상승 중 (0~8 s) | 0.0763 → 0.0690 (90.5%) | 유지 |
| 정지 후 (10 s) | **0.0053 (6.9%)** | 상실 |

#### 추가 관측 (2026-09-09) — 회신·재발송 때 쓴다

> **발송분에 포함됐는지는 확인이 필요하다.** 메일과 같은 날 측정한 것이라 들어갔을 수도,
> 안 들어갔을 수도 있다. 회신에 이 내용이 반영돼 있지 않으면 보강 자료로 보낸다.

**① 정지 후 붕괴는 훨씬 긴 이동을 견딘 뒤에도 일어난다.** 위 실측은 수직 +88 mm / 8 초인데,
아래는 그보다 큰 복합 이동을 견디고도 결국 놓쳤다. **동역학이 아니라 정지 상태의 마찰**이
문제라는 것을 더 분명히 한다.

| 구간 | peg 위치 | 그리퍼 `effort` | `at_goal` |
|---|---|---|:-:|
| 파지 직후 | (0.5024, −0.0016, 0.0249) | **23.337 N·m** | true |
| 수직 리프트 **+75.7 mm** | z 0.1006 | 20.493 | true |
| **`ready` 로 430 mm 관절공간 이동 (약 5 s)** | (0.3049, −0.0005, 0.3869) | 17.298 | **true — 유지** |
| **정지 3~8 s 뒤** | 낙하 | **0.004** | false |

**② 파지력이 연속적으로 감쇠한다 — 갑자기 미끄러지는 것이 아니다.**

```
23.337  →  20.493  →  17.298  →  0.004 N·m
파지        리프트 후    ready 후     붕괴 후
```

붕괴 전까지 **26 % 가 서서히 빠진다.** "정지마찰 부족"보다 **접촉력 자체가 시간에 따라
이완된다**는 쪽을 시사한다 — 접촉 모델의 감쇠(`D_T_V` · `C`)나 침투 해소 거동과 관련될 수
있다.

**③ 손가락은 닫힌 채로 물체만 빠져나갔다** — 명령이 풀린 것이 아니다.

```
손가락 관절  0.725 rad   (grasp 지령 그대로)
effort       2.8e-8 ~ 4.0e-5 N·m
goal         'grasp'     at_goal=false   stalled=false
```

> **묻고 싶은 것**
>
> 1. `t1` 에 손끝 접촉이 빠진 것이 의도인가, 누락인가
> 2. `r2` 의 값(`S_F_C=1`, `K=1000000`)을 `t1` 에 그대로 써도 되는가
> 3. 물체 쪽 `0.15` 와 손끝 쪽 값이 함께 걸릴 때 **어느 쪽이 적용되는가**
> 4. **(2026-09-09 추가)** 파지력이 유지 중 26 % 감쇠하다 붕괴하는데, 접촉력의 **시간
>    이완**이 모델에 있는가. 있다면 어느 파라미터가 그것을 정하는가

---

<a id="b-12"></a>
### ~~B-12.~~ 조작 대상 body 의 pose 를 **TF 로 발행**해 달라 — **✅ 해결 (2026-09-08)**

> **벤더가 요청대로 반영했다.** 새 빌드 `MMR-Build_260907` 에서 `/tf` 발행자가 생겼다
> (A-16 기준선은 발행자 0 이었다).
>
> | 수용 기준 | 결과 |
> |---|:-:|
> | `world → peg_round_16` TF | ✅ **28.75 Hz** (1725건/60초) |
> | 프레임 이름 = 모델 body 이름 | ✅ `peg_round_16` 그대로 |
> | `panda_link*` 미발행 | ✅ **고유 프레임쌍 1개**뿐 |
> | `static="false"` 인 것만 | ✅ peg 하나 (hole·tray 는 `static="true"` → 제외) |
> | `header.stamp` | ✅ **벽시계 epoch** (다른 데이터 채널과 동일). 역행 0회 · 중복 0건 |
> | 정지 동조 | ✅ A-17 에 함께 멈춘다(최대 공백 1105 ms) → 정합 유지 |
> | **쿼터니언 순서** | ✅ **ROS 규약 xyzw** — 아래 |
>
> **단위·좌표계 검증.** 재시작 직후 peg 가 XML 초기 자세로 돌아온 순간을 잡아 대조했다.
> `t1__floating_2_vm.xml` 의 `origin xyz="0.5 -0.1 0.028001" rpy="0 0 0"` 에 대해 TF 가
> **`(+0.500000, -0.100000, +0.028001)`** 을 냈다 — **소수 6자리 일치**. 미터 단위 · world
> 기준이 확정된다.
>
> **쿼터니언 검증 (요청서 §6 의 "알려진 자세" 방법).** 같은 순간의 회전은
> **`(-0.000000, +0.000000, -0.000000, -1.000000)`** 이었다. `xyzw` 로 읽으면 회전각
> **0.000°**(항등), `wxyz` 로 읽으면 180.000° 다. **따라서 ROS 규약 xyzw 이며 변환 코드가
> 필요 없다.**
>
> ⚠️ **`w` 가 음수로 온다.** `q` 와 `−q` 는 같은 회전이므로 수학적으로 정상이고 tf2 도
> 정상 처리하지만, **`w ≥ 0` 을 가정하거나 성분을 그대로 비교해 항등을 판정하는 코드는
> 오판한다.** `lookup_transform` 결과를 그대로 싣는 한 안전하다.
>
> ⚠️ **XML 에 TF 설정 요소가 없다 — 벤더가 코드로 구현했다.** 즉 **우리가 대상 body 를
> 고를 수 없다.** 조작 대상이 여러 개가 되면 다시 문의해야 한다.
>
> ⚠️ **`/clock` 과의 시간축 문제는 그대로다** (B-13). TF 가 늘어난 만큼
> `use_sim_time` 을 켰을 때 깨질 대상도 하나 늘었다.
>
> 아래는 요청 원문이다.

#### (원문) 조작 대상 body 의 pose 를 TF 로 발행해 달라 — 2026-09-04

**필요한 이유.** 우리 수집 계층은 물체 상태를 `rdfp_msgs/SceneObjects` 계약으로
`/scene/objects` 에 실어 rosbag2 에 기록한다. 이것이 없으면 **데이터셋에 물체 위치가
남지 않아 에피소드의 성패를 관측할 수 없다** — 팔이 어떻게 움직였는지는 알지만 물체가
꽂혔는지 굴러떨어졌는지는 알 수 없다. 계약은
[docs/scene/scene_objects_guide.md](../scene/scene_objects_guide.md) 에 있다.

**요청은 한 가지다.**

> `floating_N` 조인트가 `<General static="false"/>` 인 body 의 pose 를 **TF 로 발행**해
> 달라. 부모 프레임은 `world`, 프레임 이름은 **모델의 body 이름 그대로**.
> 현재 `t1` 씬에서는 **`peg_round_16` 하나**다.
>
> **로봇 링크(`panda_link*`) TF 는 발행하지 말아 달라** — 우리 쪽
> `robot_state_publisher` 가 같은 프레임을 발행하고 있어 충돌한다.

**대상 선별 기준이 이미 모델 안에 있다.** 우리가 필요한 것은 **조작 대상뿐**이고,
그 구분은 `floating_N` 의 `static` 속성과 정확히 일치한다.

| VariableModel | child link | `static` | 우리에게 필요한가 |
|---|---|:-:|:-:|
| `t1__floating_1_vm.xml` | `peg_hole_round_16` | `true` | 아니오 (고정 환경) |
| **`t1__floating_2_vm.xml`** | **`peg_round_16`** | **`false`** | **예** |
| `t1__floating_3_vm.xml` | `peg_tray_round_16` | `true` | 아니오 (고정 환경) |

**pose 만 주시면 된다.** 계약의 나머지 필드는 시간에 따라 변하지 않으므로 저희가 채운다.

| 필드 | 누가 | 값 / 근거 |
|---|---|---|
| `name` | 저희 | body 이름과 매핑만 한다 |
| `type` | 저희 | `cylinder` |
| `dimensions` | 저희 | `[0.05, 0.008]` — `assets/industrealkit/peg_round_16mm.obj` 의 bbox 에서 산출 (16.0 mm × 50.0 mm) |
| **`pose`** | **벤더** | **매 프레임 변한다. 이것만 필요하다** |
| `header` | 저희 | 저희 노드가 stamp 와 `panda_link0` 를 채운다 |

**왜 pose 토픽이 아니라 TF 인가.** TF 로 주시면 조회 한 번에 **world → 로봇 베이스
좌표 변환과 쿼터니언 성분 순서가 동시에 해결**되어, 저희 쪽 변환 코드가 사라진다.
pose 토픽으로 주시면 **기준 프레임의 원점·축 방향과 쿼터니언 순서(xyzw / wxyz)를
문서로 명시**해 주셔야 하므로 오히려 작업이 늘어난다.

이 축에서 이미 한 번 막힌 전례가 있다 — `/output/endeffector` 는 `header.frame_id` 가
빈 문자열이고 쿼터니언 순서가 미정이어서 값을 해석할 수 없었다. **순서를 틀려도 노름이
1 이라 어떤 검사도 통과하고 '그럴듯하게 틀린 자세'가 나오는** 종류의 오류라, 변환
코드를 두지 않는 편이 안전하다.

**tf2 배선은 이미 있는 것으로 보인다.** `/tf` 에 `tf_RosSubscriber` 노드가 붙어 있어
메시지 타입과 경로는 갖춰져 있고, **발행 방향만 없다** (관측: Unity 측 `/tf` 발행자 0).

**수용 기준.**

```bash
ros2 run tf2_ros tf2_echo panda_link0 peg_round_16   # 변환이 나온다
ros2 topic info /tf --verbose | grep Publisher       # Unity 측 발행자가 생겼다
```

- 프레임 이름이 모델의 body 이름과 **동일** (`peg_round_16`)
- **`panda_link*` 는 발행되지 않는다**
- 발행 주기는 기존 `<Output ... Hz="30">` 와 같으면 충분하다 (저희 노드가 2 Hz 로 재발행)
- 회전 검증은 노름이 아니라 **알려진 자세**로 한다 — 회전이 없는 peg 가 `xyzw = (0,0,0,1)`
  로 나오는지, z축 90° 같은 **비대칭** 회전이 맞게 나오는지

> **묻고 싶은 것**
>
> 1. `static="false"` 인 body 를 TF 로 내보내는 것이 설정으로 가능한가, 빌드 수정이 필요한가
> 2. 발행할 body 를 **선택**할 수 있는가 (전체가 아니라 조작 대상만)
> 3. 프레임 이름 규약을 정할 수 있는가 (body 이름 그대로가 저희에게 가장 좋다)

**별도 요청으로 분리한 것 — 런타임 배치 변경.** 에피소드마다 물체 배치를 바꾸려면
런타임에 body pose 를 설정하는 수단이 필요한데, 현재는 `floating_N` 의
`<origin xyz rpy>` 를 고쳐 **재시작**하는 방법뿐으로 보인다. **이번 요청에는 넣지
않는다** — 고정 배치로도 수집이 가능하고, 최소 요청을 유지하는 편이 낫다. 무작위 배치가
필요해지는 시점에 별건으로 요청한다.

---

<a id="b-13"></a>
### B-13. `/clock` 과 `/output/*` 의 **시간축을 하나로** 맞춰 달라 — 2026-09-08

**새 빌드에서 `/clock` 발행이 시작됐다.** 감사하지만 **현재 형태로는 켤 수 없다.**

| 채널 | 시간 기준 | 실측값 |
|---|---|---|
| **`/clock`** | **기동 후 경과시간** | 2791.7 s (기동 46분 시점) |
| `/output/panda_joint` · `gripper_joint` · `endeffector` | **벽시계 epoch** | 1788838268 s |
| `/camera_image/compressed` | **벽시계 epoch** | 동일 |

**같은 시스템에 두 시간축이 있고 차이가 56.7년이다.** ROS 2 에서 `use_sim_time:=true` 를
켜면 우리 노드는 `/clock` 을 현재 시각으로 쓰는데, 시뮬레이터가 보내는 메시지의
`header.stamp` 는 계속 벽시계다. 그러면 TF 조회가 성립하지 않고, 데이터셋의 세션 시작
시각이 1970년이 되며, 에피소드 키가 재시작마다 충돌해 **에러 없이 조용히 건너뛴다.**

**요청** — 둘 중 하나로 통일해 달라. 어느 쪽이든 좋다.

1. **전부 벽시계 epoch** — `/clock` 도 epoch 을 싣는다. 우리는 `use_sim_time` 을 끈 채
   지금처럼 쓰고, `/clock` 은 계측용으로만 본다. **우리에게 가장 간단하다.**
2. **전부 sim 시각** — `/output/*` 와 카메라의 `header.stamp` 도 `/clock` 과 같은
   경과시간으로 찍는다. 이러면 `use_sim_time:=true` 를 켤 수 있다.

**섞인 상태가 가장 나쁘다** — 어느 쪽 규약으로 코드를 써도 절반이 틀린다.

**수용 기준.** 같은 순간에 받은 `/clock` 값과 `/output/panda_joint` 의 `header.stamp` 차이가
**1초 이내**일 것 (현재는 1.79 × 10⁹ 초).

> **묻고 싶은 것**
>
> 1. 어느 쪽으로 통일하는 것이 그쪽 구현에 자연스러운가
> 2. `/clock` 의 증분이 13.89 ms 인데, 이것이 **시뮬레이션 내부 스텝**인가
> 3. **lockstep**(시뮬레이터가 구독자를 기다리는 모드)을 지원하는가 — `/clock` 발행과는
>    별개 기능이며, 지원한다면 접촉 동역학 재현성에 크게 도움이 된다

---

<a id="b-8"></a>
### B-14. `r2` 씬에 **peg ↔ 탁자 접촉**을 추가해 달라 — 2026-09-11

> **솔버** RecurDyn (`r2`). 표면 정의는 이미 빌드 안에 있으므로 **접촉 쌍 한 줄**이면 된다.

**증상.** 고정물(`peg_hole` · `peg_tray`) 밖에서 그리퍼를 열면 **peg 이 탁자를 통과해
무한히 떨어진다.** 3회 재현했고 z 가 −1103 m · −3615 m · −3900 m 까지 갔다.
펑션베이에는 `/scene/reset` 이 없어 **복구가 시뮬레이터 재시작뿐**이다.

**원인.** `r2` 씬의 접촉 정의가 **넷뿐이고 전부 peg 을 한쪽으로 한다.**

```
r2__GGeomContact_1   peg_round_16_surf ↔ left_inner_finger_surf
r2__GGeomContact_2   peg_round_16_surf ↔ right_inner_finger_surf
r2__GGeomContact_3   peg_round_16_surf ↔ peg_hole_round_16_surf
r2__GGeomContact_4   peg_round_16_surf ↔ peg_tray_round_16_surf
```

**`peg ↔ 탁자` 가 없다.** 그래서 peg 에게 탁자는 **물리적으로 존재하지 않고** 손가락·구멍·
트레이 중 무엇도 잡아 주지 않는 순간 자유낙하한다.

**튕겨 나가는 것이 아니라 그냥 떨어진다** — 개방 직후 낙하를 재면 가속도가
**−11.25 m/s²** 로 중력과 같다 (−199.7 → −561.5 → −1103.5 m, 4초 간격).

**요청.** `GGeomContact_3`/`_4` 와 같은 형태로 하나 더 추가해 달라.
**표면은 이미 있다** — `ConstantModel/geosurfs/ground_table_surf.xml` (`PART="ground"`,
평면, `z=+720.2 mm`, `x [387, 1187]` · `y [-1353.5, 446.5]` mm). 우리 작업 영역
(`x 0.40~0.65`, `y -0.15~0.15` m)을 덮는다.

```xml
<!-- r2__GGeomContact_5_vm.xml (제안) -->
<Force name="GGeomContact_5" type="Contact">
  <child  IGEOSURFNAME="peg_round_16_surf" />
  <friction D_F_C="0.1" S_T_V="0.2" D_T_V="0.3" S_F_C="0.1" />
  <mesh IDIRECTION="UP" JDIRECTION="UP" INODECONTACT="1" JNODECONTACT="0" NOMAXCP="30" />
  <normal K="200000" C="20000" MAXPEN="1" />
  <parent JGEOSURFNAME="ground_table_surf" />
</Force>
```

**왜 필요한가.** 지금은 **peg 을 집어서 탁자에 내려놓는 것이 불가능하다.** 구멍과
트레이 두 자리 사이를 오가는 것만 된다. 일반적인 pick-and-place — 임시로 내려놓기,
재파지, 여러 물체 정렬 — 가운데 어느 것도 성립하지 않고, 시도하면 **물체를 영구히
잃는다.**

**우리 쪽에서 배제한 것.** 원인을 세 번 잘못 짚었고, 그 셋이 전부 아니었다.

| 가설 | 반증 |
|---|---|
| peg 이 기울어서 | **0.54°** 로 똑바로 선 채 놓아도 떨어졌다 |
| 눌린 채 놓아서 | 바닥을 **+0.26 mm** 띄워 접촉을 푼 뒤에도 떨어졌다 |
| 개방이 급해서 | **2초 램프**(50 Hz 보간)로 놓아도 떨어졌다 |
| 놓는 로직이 잘못돼서 | 같은 로직이 구멍·트레이에서는 **왕복 5회 연속 성공**한다 |

세 번 모두 공통점은 **고정물 밖에서 놓았다**는 것 하나였다.

**수용 기준.**

1. `ready` 에서 peg 을 집어 탁자의 빈 자리(예: `(0.60, 0.00)`)로 옮기고, peg 바닥이
   탁자면에 닿은 상태에서 그리퍼를 열었을 때 **peg 이 그 자리에 남는다**
   (`/scene/objects` 의 `peg.pose.position.z` 가 **0.025 ± 0.005 m** 로 유지).
2. 10초 뒤에도 같은 값이다 (천천히 가라앉지 않는다).
3. 이어서 그 peg 을 다시 집어 `peg_hole` 에 꽂을 수 있다.

---

### B-8. Panda 링크 메시 `link0~7.obj`

**무엇을** — 제공해 주신 URDF 가 참조하는 다음 8개 파일을 함께 주시기 바란다.

```
package://panda_ftsensor_robotiq/meshes/collision/link0.obj ~ link7.obj
```

**지금 무슨 일이 벌어지나 (2026-09-01 실측).** URDF+SRDF 를 MoveIt 에 로드하면
모델 자체는 뜨지만 팔 링크 8개가 이렇게 처리된다.

```
[WARN] Link panda_link0 has visual geometry but no collision geometry.
       Collision geometry will be left empty.
```

**팔의 충돌 형상이 비어 있다.** 그 상태로는 자기충돌·환경충돌 검사가 팔에 대해
사실상 꺼지므로, 계획은 늘 성공하고 실행에서 부딪힌다.

**왜 우리가 만들 수 없나** — 같은 부품의 메시가 `moveit_resources_panda_description`
에 STL 로 있긴 하지만, **그것이 시뮬레이터가 쓰는 기하와 같다는 보장이 없다.** 다르면
MoveIt 의 충돌 검사가 실물과 어긋나는데, 계획은 성공하고 실행에서만 부딪히므로
**에러 없이 드러나지 않는다.** 우리가 임의로 대체하면 그 차이가 데이터에 조용히 남는다.

Robotiq 쪽 메시 10종(coarse + fine)은 이미 받아 `panda_ftsensor_robotiq` 패키지에
정리했다. **남은 것은 이 8개뿐**이다.

**수용 기준** — `panda_ftsensor_robotiq` 패키지에 넣고 URDF 를 로드했을 때 메시 참조
18종이 모두 해석되고, RViz 에서 팔 형상이 시뮬레이터 화면과 일치한다.

<a id="b-7"></a>
### ~~B-7. 시뮬레이터 머신 시계 동기~~ — **폐기 (2026-09-03)**

> 시뮬레이터가 같은 머신으로 옮겨졌고, 2026-09-03 재측정에서 stamp 가 **wall-clock**
> 으로 확인됐다. 오프셋이 흔들리는 것처럼 보이던 현상은 **시계 차이가 아니라
> A-5 의 정지+과속 톱니**였다 — 창을 어디에 걸치느냐에 따라 평균이 −0.06 s 도
> −0.65 s 도 나왔던 것이다. **이 항목은 발송하지 않는다.** 아래는 이력이다.

#### (이력) 시뮬레이터 머신 시계 동기

시뮬레이터가 찍는 스탬프가 호스트 시각보다 **일정하게 2.22 초 뒤처져 있다**
(open_work §6.4).

| 토픽 | 스탬프 − 수신시각 | 변동폭 |
|---|---|---|
| `/output/panda_joint` | **-2.2207 s** | 0.0171 s |

드리프트가 아니라 고정 오프셋이고 변동폭이 작으므로 **두 머신의 시계 차이**로 본다
(스탬핑 버그가 아니다). **시뮬레이터 머신에 NTP/chrony 를 걸어 주면 해결된다.**

우리 쪽 우회(`stamp_source:=now` 로 재스탬프, 오프셋 -0.0009 s)가 있지만, 그러면
스탬프의 의미가 "그 자세였던 시각"에서 "우리가 받은 시각"으로 바뀌어 dead time
(약 34 ms)이 흡수된다. **NTP 가 가능하면 그쪽이 낫다.**

---

## C. 우리가 어떻게 검증하는가

수정본을 받으면 아래를 그대로 돌려 수용 기준과 대조한다. 절차 전문은
[functionbay_open_work.md §9](functionbay_open_work.md) 에 있다.

```bash
# 0) 도메인·RMW 를 시뮬레이터와 맞춘다 (ROS_DOMAIN_ID=31 / rmw_fastrtps_cpp)
ros2_env humble
./scripts/run_panda_functionbay.sh

# 0-1) A-5 검증 — 팔을 움직이지 않고 정지 유무부터 본다. 180초는 정지 주기(34초)를
#      다섯 번 담기 위한 최소값이다. 짧으면 톱니가 상쇄되지 않아 진행률이 편향된다.
./scripts/functionbay/fb_stamp.py 180 misc/fb_stamp/<라벨>.json
#    → 수용 기준: 0.3 s 초과 수신 공백 0회, 정지 제외 진행률 1.00 ± 0.01

# 0-2) 나머지 수동 관측 (타입·필드·QoS·camera_info)
./scripts/functionbay/fb_checkup.py 180
#    → 시뮬레이터 XML 설정을 바꿨으면 --joint-hz N --cam-hz N 로 알려준다

# 1) 명령에 반응하는가 — 가장 먼저 (A-4 판정도 겸한다)
./scripts/functionbay/fb_raw.py named 0.30 4.0

# 2) A-1 검증 — 현재 자세를 그대로 명령해 정착 오차를 잰다
./scripts/functionbay/fb_hold.py 15
#    → 자세를 ready / 중간 / 펴짐 세 가지로 바꿔 가며 반복한다

# 3) B-1 검증 — joint7 계단 응답으로 dead time · τ 를 분해한다
./scripts/functionbay/fb_probe.py 30 /tmp/probe.json &
./scripts/functionbay/fb_move.py named extended 50.0 /tmp/start.json

# 4) 이상 자세 복구
./scripts/functionbay/fb_recover.py 5.0
```

> `fb_hold.py` 는 **현재 자세를 읽어 그대로 목표로 준다.** 처짐이 있는 상태에서
> 반복 호출하면 값이 누적되므로(open_work §10.2), 자세를 바꿀 때는 `fb_raw.py` 나
> `fb_recover.py` 로 절대 목표를 먼저 준다.

---

## D. 요청하지 않는 것 — 이미 정상인 항목

혼선을 막기 위해 **문제 없음이 실측으로 확인된 것**을 함께 적는다.

| 항목 | 실측 |
|---|---|
| 명령 경로 | `/input/panda_joint` **50.43 Hz** 도달, 명령대로 이동 (joint7 +0.30000 → +0.29999). *(보고 50 Hz 시절 값. 현재 설정은 30 Hz 이고 2026-09-04 재확인에서 7/7 관절 반응·이동률 95~102%)* |
| 상태 보고 | `/output/panda_joint` **50.24 Hz**, `/joint_states` 49.8 Hz |
| 관절 이름 | 시뮬레이터가 `name` 을 **채워서 보낸다** (`panda_joint1~7`, 그리퍼는 `gripper_joint1~6`) |
| 그리퍼 채널 | `/output/gripper_joint` **30 Hz**, 6관절 × 각도/각속도/토크. 명령 추종 오차 2e-05 (backend_design §6.1) |
| QoS | 양쪽 RELIABLE / VOLATILE / KEEP_LAST — 호환 |
| 실시간 배속 | **0.99993** (2026-09-08 `/clock` 기준 **0.99975**) — 평균은 정확히 실시간. ⚠ **다만 균일하지 않다** — 주기적으로 멈추고 그 사이 과속한다(**A-5**). 평균이 1.0 인 것과 속도가 일정한 것은 다르다. ⚠ **`/clock` 은 2026-09-08 새 빌드에서 발행이 시작됐다** — 아래 주 참조 |
| 네트워크 | 왕복 0.303 ms — 전체 응답(약 180 ms)의 0.2%. **머신 배치는 무관하다** |
| 카메라 렌더링 | 실시간 렌더링 확인 (프레임 해시가 전부 다름). 주기는 **XML 설정값**이며 10→5 Hz 반영을 확인했다 (~~B-10~~ 해결) |
| 고율 발행 내성 | 500 Hz × 15 초를 견딘다 |

> ### ⚠️ `/clock` 은 2026-09-08 새 빌드에서 발행이 시작됐다 — 이 절의 서술을 정정한다
>
> 아래 원문은 "**`/clock` 발행은 요청 대상이 아니다**" 였고, 그 판단의 **근거는 지금도
> 유효하다** — 이미 1.0 배속이고 lockstep 이 아니므로 `/clock` 만으로는 얻는 것이 없다.
> **실제로 벤더가 발행을 시작했지만 우리는 켤 수 없다.**
>
> 이유는 성능이 아니라 **시간축이 섞였다**는 것이다. `/clock` 은 **기동 후 경과시간**
> (2791.7 s)인데 `/output/*` 와 카메라는 **벽시계 epoch**(1788838268 s)다 — 차이 **56.7년**.
> `use_sim_time:=true` 를 켜면 우리 노드는 `/clock` 을, 시뮬레이터 메시지는 벽시계를 쓰게
> 되어 TF·데이터셋이 §A-9 에 기록된 대로 파손된다.
>
> **따라서 요청은 "발행해 달라"가 아니라 "하나로 맞춰 달라"로 바뀐다 (신규 B-13).**
> 어느 쪽으로 통일해도 좋다 — 전부 벽시계이거나 전부 sim 시각이면 된다.
> **섞인 상태가 가장 나쁘다.**
>
> `/clock` 에는 **뜻하지 않은 쓸모**가 생겼다 — 71.94 Hz · 증분 13.89 ms(내부 스텝 ~72 Hz)
> 라 **A-5 정지를 직접 계측**할 수 있다. 공백 0.74 s 동안 sim 시각이 0.10 s 만 흐른다.

아래는 원문이다. 특히 **`/clock` 발행(sim time)은 요청 대상이 아니다** — 이미 1.0 배속이라 도입해도
servo 안정성이 나아지지 않는다 (open_work §6.3). sim time 이 의미를 갖는 것은
시뮬레이터가 **lockstep(스텝 후 대기)** 을 지원할 때뿐이므로, 그 얘기를 꺼낼 때는
"`/clock` 발행"과 "lockstep 지원"을 반드시 구분해서 말해야 한다.


---

<a id="closed"></a>
## 부록 E. 닫힌 항목 — **번호는 유지한다**

요약(§0)에서 뺀 항목이다. **번호를 재사용하지 않는 이유**는 벤더 회신과 지난 메일이
옛 번호로 지칭하기 때문이다 — `B-5` 가 다른 것을 가리키게 되면 주고받은 기록이 어긋난다.

각 항목의 **본문 절은 그대로 남아 있다** (취소선 제목). 근거 수치와 당시 판단은 거기 있고,
아래 표는 "무엇이 · 언제 · 왜" 닫혔는지만 요약한다.

| | 항목 | 솔버 | 종결 | 시점 | 근거 |
|:-:|---|:-:|---|---|---|
| **[A-3](#a-3)** | 카메라 `header.stamp` | Both<br>*(솔버 무관)* | **해결** | 2026-09-03 | 전 토픽 wall-clock 으로 통일. 카메라−관절 차 **−13.4 ms 고정**(발행 주기를 3배 바꿔도 불변)이라 우리가 상수로 보정한다 |
| **[B-1](#b-1)** | `K_p` 적정 상향 | **Both** | **강등** | 2026-09-04 | τ **199.9 ms**(기준선 252~259)로 개선 확정. §0.1 의 "τ 242 ms 악화" 판정은 **무효**다. twist 경로도 `servo_command_bridge` + 스케일 0.8 로 연결됐다 |
| **[B-3](#b-3)** | 관절 한계 클램프 | **RecurDyn** | **해결 유지** | 2026-09-04 | **Crisp 에서는 처음부터 문제가 아니었다** — joint4 상한 **+0.0698** 을 4자리까지 재현(클램프가 동작한다). RecurDyn 에서 `-3.36` 이 그대로 통과하던 것이 원래 근거다. joint7 이 URDF 를 넘는 문제는 **B-9 로 이관** |
| **[B-4](#b-4)** | `/output/endeffector` 값 규약 | **Both** | **철회** | 2026-09-04 | 발행은 재개됐으나 **우리 스택이 이 값을 쓰지 않는다.** `/ee_pose` 를 `/output/panda_joint` → FK 로 직접 계산한다 — 규약을 알아도 쓸 데가 없다 |
| **[B-5](#b-5)** | 카메라 `rgb8` 발행 | Both<br>*(솔버 무관)* | **해결** | 2026-08-31 | `rgba8` → **`bgr8`** 전환 확인 (step 2560→1920, 대역폭 −25%). `cv_bridge` 기본 형식이라 변환이 불필요해졌다 |
| **[B-6](#b-6)** | 그리퍼 출력 사양 | **t1 한정** | **해결 (범위 정정)** | 2026-09-01 / 09-10 | `sensor_msgs/JointState` 6관절 × q/v/f 로 확정. **형식이 솔버 교체와 함께 바뀌었다** — 옛 `Float64MultiArray` 6개(RecurDyn)는 2 finger × (q,v,f) 였음이 함께 해석됐다 (backend_design §6.1) |
| **[B-7](#b-7)** | 시뮬레이터 머신 시계 동기 | Both<br>*(솔버 무관)* | **폐기** | 2026-09-03 | 같은 머신이고 stamp 는 wall-clock 이다. 2.22 s 오프셋이 흔들리던 **진짜 원인은 시계가 아니라 A-5 정지**였다 |
| **[B-10](#b-10)** | 카메라 주기 설정 | Both<br>*(솔버 무관)* | **해결** | 2026-09-03 | 9.606 Hz 고정이 아니라 **XML 설정값**이었다 — `t1__RGBCamera_1_vm.xml` 의 `Publish_Frequency`·`Frequency` 를 10→5 로 바꾸니 4.98 Hz. **애초에 요청할 필요가 없었다** |
| **[B-12](#b-12)** | 조작 대상 body 의 pose 를 TF 로 발행 | **Crisp** | **✅ 해결** | 2026-09-08 | `world → peg_round_16` **28.75 Hz**, 벽시계 stamp, 쿼터니언 **ROS xyzw**. 요청 조건을 전부 충족했다. 요청 원문·검증은 [B-12](#b-12) |

### 종결 유형이 네 가지인 것에 주의한다

| 유형 | 뜻 | 다시 열릴 수 있는가 |
|---|---|---|
| **해결** | 벤더가 고쳤고 실측으로 확인했다 | 회귀하면 — B-4 가 한 번 그랬다(발행 재개 → 무발행 → 재개) |
| **강등** | 문제는 남아 있으나 **요청할 만큼은 아니다** | 수치가 다시 나빠지면 |
| **철회** | 우리 쪽 설계가 바뀌어 **필요가 없어졌다** | 설계를 되돌리면 |
| **폐기** | **애초에 잘못 짚었다** — 원인이 다른 데 있었다 | 아니다 |

**"해결"과 "폐기"를 섞어 읽으면 안 된다.** B-7 은 벤더가 고친 것이 아니라 우리가 원인을
잘못 짚은 것이고(실제 원인은 A-5), B-10 도 요청 대상이 아니라 **설정 항목**이었다.
같은 종류의 오진을 반복하지 않으려면 그 구분이 남아 있어야 한다.


---

<a id="solver"></a>
## 부록 F. 솔버별 귀속 판단 근거

시뮬레이터는 솔버를 두 가지 쓴다. 요청 항목이 **어느 쪽에서 관측된 것인지**를
명시하지 않으면 벤더 쪽에서 어느 코드를 봐야 할지 알 수 없다.

| 문서 표기 | 정식 명칭 | 사용 시기 |
|---|---|---|
| 펑션베이 솔버 | **RecurDyn** | ~ 2026-08-28 (1·2차 측정) |
| 서울대 솔버 | **Crisp** | 2026-08-31 ~ (3차 측정 이후 **현재까지**) |

**측정 회차가 곧 솔버다.** 이것이 귀속의 1차 근거다.

| 회차 | 날짜 | 솔버 |
|:-:|---|---|
| 1차 | 2026-08-18 | RecurDyn |
| 2차 | 2026-08-28 | RecurDyn |
| **3차** | **2026-08-31** | **Crisp — 이 측정 직전에 교체됐다** |
| 이후 전부 | 2026-09-01 ~ | Crisp |

> **2026-09-01 이후에 도출된 항목은 예외 없이 Crisp 다** (A-5 · B-9 · B-11 · B-12 ·
> B-13 · `effort` 문의). 그 이전 항목만 회차를 따져 갈랐다.

### 표기의 뜻

| 표기 | 뜻 |
|---|---|
| **Crisp** | Crisp 에서만 관측됐다. RecurDyn 시절에는 없었거나 측정되지 않았다 |
| **RecurDyn** | **RecurDyn 에서만 문제였다.** Crisp 에서는 정상임이 확인됐다 — 되돌아갈 때만 유효한 항목이다 |
| **Both** | 두 솔버 모두에서 관측됐다 (수치는 다를 수 있다) |
| **Both *(솔버 무관)*** | 솔버 영역이 아니다 — 카메라 발행 계층 · 머신 시계 · URDF 에셋. **솔버 담당이 아니라 다른 팀이 볼 항목이다** |

**`Both` 두 종류를 섞어 읽으면 안 된다.** 앞은 "두 솔버가 같은 증상을 낸다"이고
뒤는 "솔버와 무관하다"이다 — 벤더 쪽에서 **어느 팀이 볼 것인가**가 갈린다.

### 항목별 근거

| | 솔버 | 근거 |
|:-:|:-:|---|
| **[A-1](#a-1)** | Both | RecurDyn joint4 **−0.0983 rad**, Crisp **−0.0097 rad** (§0.1). 처짐은 10배 줄었으나 **방향 의존은 Crisp 에서도 남았다** (09-01·09-03 실측, 비대칭 9.87 mm) |
| **[A-2](#a-2)** | Both | 제어기 사양은 솔버마다 다르므로 **쓰는 쪽의 것**을 받아야 한다. **지금 필요한 것은 Crisp** — RecurDyn 으로 되돌아가면 그쪽 사양도 필요해진다 |
| **[A-3](#a-3)** | Both *(솔버 무관)* | 카메라 발행 계층. 1·2차(RecurDyn) `stamp=0`, 3차(Crisp)에서도 그대로였고 09-03 에 해결 — **교체와 무관하게 같았다** |
| **[A-4](#a-4)** | Both | 2차(08-28, RecurDyn)에서 관측되고 **3차(08-31, Crisp)에서도 재현**됐다 (§0.1). 09-04 재판정(Crisp)에서는 재현되지 않았다 |
| **[A-5](#a-5)** | Crisp | 2026-09-03 발견 |
| **[B-1](#b-1)** | Both | RecurDyn τ **133 ms** → Crisp τ **242 ms** → **199.9 ms** (§0.1·§0.3). 두 솔버 모두에서 측정됐다 |
| **[B-2](#b-2)** | Both | B-1 과 짝이다. 어느 솔버든 게인을 밖에서 만질 수 있어야 한다 |
| **[B-3](#b-3)** | **RecurDyn** | 근거는 2차(RecurDyn) 시험 — joint4 에 `-3.36`(URDF 한계 −3.1416 초과)을 명령했더니 **그대로 갔다** (open_work §2.2). **Crisp 에서는 +0.0698 에서 클램프된다** (09-04, 4자리 재현) — 그쪽에서는 처음부터 문제가 아니었다 |
| **[B-4](#b-4)** | Both | RecurDyn 토픽 없음 → Crisp 발행 확인 → 09-01 무발행 회귀 → 09-03 재개 (§0.1·§0.2) |
| **[B-5](#b-5)** | Both *(솔버 무관)* | 카메라 픽셀 형식. 08-31 에 `rgba8`→`bgr8` 전환 확인 |
| **[B-6](#b-6)** | Both | **형식이 솔버 교체와 함께 바뀌었다** — RecurDyn `Float64MultiArray` 6개 → Crisp `sensor_msgs/JointState` 6관절 (§0.1) |
| **[B-7](#b-7)** | Both *(솔버 무관)* | 머신 시계. 교체 전 관측이고, 원인은 시계가 아니라 A-5 였다 |
| **[B-8](#b-8)** | Both *(솔버 무관)* | URDF 가 참조하는 메시 파일이 없다 — 에셋 문제다 |
| **[B-9](#b-9)** | Crisp | 09-03·09-04 실측 (joint7 상한 **+3.1500** > URDF 2.9671, joint4 는 URDF 안쪽 **+0.0698**) |
| **[B-10](#b-10)** | Both *(솔버 무관)* | 카메라 XML 설정값(`Publish_Frequency`)이었다 |
| **[B-11](#b-11)** | **Crisp** | 09-04 관측 — `t1` 씬에 손끝 접촉 정의가 없어 파지 후 이동에서 놓친다 |
| **[B-12](#b-12)** | Crisp | 09-04 제기, 09-08 해결 |
| **[B-13](#b-13)** | Crisp | 09-08 — `/clock` 발행이 시작되며 드러났다 |
| [**effort 의미**](#a-4) | Crisp | 09-04 — `Kp × 잔차`(모델 `Kp=200`)임이 확인됐다 |

### 판단이 확정되지 않은 것

| | 사정 |
|:-:|---|
| **B-9** | Crisp 에서만 측정했다. **관절 한계가 솔버 설정인지 모델(`t1`) 속성인지 우리는 모른다** — 모델 쪽이면 RecurDyn 에서도 같을 것이다. 회신에서 알려 주시면 좋다 |
| **A-2** | 두 솔버의 제어기가 같은 구조인지 우리는 알 수 없다. 다르다면 **Crisp 쪽만** 주셔도 충분하다 |
| **A-4** | RecurDyn·Crisp 양쪽에서 관측됐으나 **09-04 재판정에서는 재현되지 않았다.** 두 솔버의 공통 경로(ROS 수신부)일 가능성이 있으나 확정하지 못했다 |
